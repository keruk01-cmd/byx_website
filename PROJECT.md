# BYX 웹사이트 — 인수인계 문서

> BYX 건축사사무소 포트폴리오 웹사이트.
> 이 문서 하나만 읽으면 다른 사람(또는 다른 AI)이 이어서 작업할 수 있도록 쓴 것입니다.
> 최종 갱신 2026-08-18 / 버전 v10

---

## 0. 30초 요약

| | |
|---|---|
| 무엇 | 서울 기반 건축사사무소의 포트폴리오 사이트 + 자체 CMS |
| 기술 | 의존성 없는 순수 HTML/CSS/JS **단일 파일** + Supabase (BaaS) |
| 파일 | `index.html` (약 528KB — 폰트 2종이 base64로 내장되어 있음), `vercel.json` |
| 빌드 | **없음.** 번들러·npm·트랜스파일 전부 없음. 파일을 열면 그게 사이트 |
| 배포 | GitHub → Vercel 자동 배포 |
| 데이터 | Supabase Postgres 4개 표 + Storage 1개 버킷 + Auth |
| 관리 | `사이트주소/admin` 으로 로그인 → 이후 각 페이지의 편집 버튼으로 바로 수정 |
| 디자인 | 순수 흑백. 1px 헤어라인. Anton(제목) + SUIT(본문) |

---

## 1. 왜 이런 구조인가 — 가장 중요한 제약

**클라이언트는 건축가이고, 개발 언어에 능숙하지 않습니다.**
이 한 문장이 지금까지의 거의 모든 기술 결정을 만들었습니다.

그래서 다음을 지켜주세요. 이걸 깨면 클라이언트가 사이트를 유지할 수 없게 됩니다.

1. **빌드 단계를 도입하지 마세요.** npm install, 번들러, 프레임워크 전부 금지.
   지금은 GitHub 웹 편집기에서 파일을 고치고 커밋하면 30초 뒤 반영됩니다.
   빌드가 생기는 순간 클라이언트는 아무것도 못 고칩니다.
2. **파일을 쪼개지 마세요.** 단일 파일이라 드래그 한 번으로 어디든 올라갑니다.
   유일한 예외가 `vercel.json` 이고, 그건 6줄짜리 라우팅 설정입니다.
3. **런타임 의존성을 늘리지 마세요.** 지금 외부에서 불러오는 건 supabase-js 하나뿐입니다.
   폰트조차 CDN이 아니라 파일에 내장되어 있습니다.
4. **설정은 파일 맨 위 `window.BYX_CONFIG` 한 곳에만.** 17~21번째 줄.
   62KB짜리 폰트 base64 뒤에 숨어 있으면 클라이언트가 못 찾습니다.

이 제약을 지키는 한, 나머지는 자유롭게 바꿔도 됩니다.

---

## 2. 파일 구조

```
byx-website/
├── index.html         # 사이트 전체 (2,544줄)
├── vercel.json        # /admin 경로 rewrite (6줄)
├── BYX_favicon.svg    # 파비콘 원본. 사이트에는 base64로 내장되어 있음
├── PROJECT.md         # 이 문서
├── CLAUDE.md          # Claude Code 작업 규칙
├── CHANGELOG.md       # 작업 로그 (날짜별, 최신이 위)
└── 배포-가이드.md       # 클라이언트용 배포·운영 매뉴얼 (비개발자 대상)
```

`BYX_favicon.svg` 는 **원본 보관용**입니다. 브라우저가 읽는 것은 `<link rel="icon">` 에
base64 로 내장된 사본이라, 이 파일을 고쳐도 사이트는 바뀌지 않습니다.
바꾸려면 SVG 를 다시 인코딩해 그 줄을 교체하세요 (단일 파일 원칙 — 별도 요청 금지).

### index.html 내부 지도

전체 2,544줄. 줄 번호는 대략치이니 섹션 주석(`/* ==== N. NAME ==== */`)으로 찾으세요.

| 줄 | 내용 |
|---|---|
| 1~23 | `<head>`, **`window.BYX_CONFIG`** (Supabase 설정) |
| 24~31 | `@font-face` 2개 — Anton, SUIT Variable (base64, 약 460KB) |
| 32~70 | CSS 변수 (`:root`), 리셋 |
| 71~160 | 헤더 / 슬롯머신 / 네비 / 모바일 메뉴 CSS |
| 161~320 | 페이지 셸, 타이포 스케일, PROJECT 그리드·인덱스 CSS |
| 321~440 | 읽기 칼럼(`.art`), 인라인 마크, **포스트 커버**, **스와이프 묶음** CSS |
| 441~540 | ABOUT/CONTACT, 폼, ADMIN(편집바·툴바·글쓰기 칸·미리보기), 푸터 CSS |
| 541~580 | 반응형 (880px / 520px 두 구간) |
| 581~634 | `<body>` 마크업 — 헤더·모바일메뉴·`#app`·푸터 |
| 637 | 0. 유틸 (`esc`, `rich()` 인라인 마크, `dnum`/`ymNum`/`ymText`, `pairs`, PRNG) |
| 706 | 1. **절차적 흑백 아트** (`svgArt`) |
| 773 | 2. 시드 데이터 + `DEFAULT_ABOUT` / `DEFAULT_CONTACT` |
| 912 | 3. **데이터 레이어** — `shrink()` 이미지 축소, `DB` 객체 |
| 1097 | 4. **슬롯머신** |
| 1174 | 5. 해시 라우터 + `editBar()` / `gotoAdmin()` |
| 1251 | 6. 공개 뷰 — `postBodyHTML()` / `galleryHTML()` / `viewPost()` |
| 1517 | 7. **ADMIN** (목록·에디터·설정 폼·글쓰기 칸·툴바·미리보기) |
| 2044 | 8. 페이지 바인딩 + `bindGalleries()` |
| 2114 | 9. 헤더 동작 + 부트 |

---

## 3. 아키텍처

### 3-1. 라우팅
해시 라우터입니다. `#/project`, `#/blog/{slug}` 형태.

```
#/                     → 프로젝트 그리드 (= 홈)
#/project/{code}       → 프로젝트 상세
#/news
#/blog  /  #/blog/{slug}
#/about
#/contact
#/admin                → 관리자
```

`parseHash()` 가 해시를 `{page, arg}` 로 파싱하고, `render()` 가 `viewXxx()` 함수의
문자열을 `#app.innerHTML` 에 넣은 뒤 `bindPage()` 로 이벤트를 붙입니다.
가상 DOM 없음. **모든 뷰는 템플릿 리터럴을 반환하는 순수 함수**입니다.

`/admin` 만 진짜 경로처럼 동작합니다. 부트 시점에 `location.pathname` 이 `/admin` 으로
끝나면 `history.replaceState` 로 `/#/admin` 으로 넘깁니다. `vercel.json` 의 rewrite가
그 경로에서 index.html을 내려주는 역할입니다.

### 3-2. 데이터 레이어 — `DB` 객체
Supabase가 있든 없든 **똑같은 API**를 제공합니다. 이게 이 코드의 핵심 설계입니다.

```js
DB.projects / DB.news / DB.posts   // 배열
DB.settings                        // {about:{…}, contact:{…}} — 페이지 글
DB.demo                            // sb === null 이면 true
DB.lastError                       // 왜 연결이 안 됐는지
DB.settingsError                   // settings 표가 없을 때만 채워짐
await DB.init()                    // 부트 시 1회
await DB.refresh()                 // 3개 표 + settings 다시 읽기
await DB.save(table, row)          // upsert
await DB.remove(table, id)
await DB.saveSettings('about', obj)
await DB.upload(file)              // → 공개 URL (데모에선 data URL). 항상 shrink() 먼저
await DB.login(email, pw) / DB.logout()
```

`settings` 는 **없어도 되는 표**로 다뤘습니다. 조회 실패는 `DB.settingsError` 에만
기록하고 기본값(`DEFAULT_ABOUT` / `DEFAULT_CONTACT`)으로 넘어갑니다. 표가 없다고
사이트 전체가 데모 모드로 떨어지면 안 되기 때문입니다.

**데모 모드**: `CFG.url`/`CFG.key` 가 비었거나 연결에 실패하면 `sb = null` 이 되고
`SEED_*` 배열을 메모리에 복사해 씁니다. 편집도 되지만 새로고침하면 초기화됩니다.
덕분에 파일을 그냥 더블클릭해도 완성된 사이트가 보이고, 기능을 전부 시험할 수 있습니다.
**이 폴백은 유지해주세요.** 클라이언트가 로컬에서 확인할 때 쓰는 유일한 수단입니다.

연결 실패 시 `DB.lastError` 에 메시지를 담고, ADMIN 상단 경고 상자에서
`hintFor()` 가 "표가 아직 없습니다 → SQL 실행하세요" 같은 한국어 해결책을 붙여 보여줍니다.

### 3-3. 무한 스크롤
`PV.shown` 만큼만 카드를 그리고, `#more` 센티넬을 `IntersectionObserver` 로 감시하다가
`BATCH`(6개)씩 `insertAdjacentHTML` 로 **덧붙입니다**. 전체 재렌더가 아니라서
스크롤 위치가 튀지 않습니다. INDEX(표) 뷰는 전체를 한 번에 그립니다 — 표는 훑어보는
화면이라 끊기면 오히려 불편합니다.

---

## 4. 디자인 시스템

### 4-1. 색
**순수 흑백만.** 회색은 칠하지 않고 이미지가 스스로 만들게 합니다.

```css
--bg:    #ffffff
--fg:    #000000
--mute:  #8e8e8e   /* 보조 텍스트 */
--faint: #d8d8d8   /* 내부 구분선 */
--hair:  1px       /* 모든 경계선의 두께 */
```

구분선은 전부 1px입니다. 그리드는 `gap: 1px` + 검은 배경으로 만들어서
셀 사이에 헤어라인이 생기게 했습니다 (`.grid`, `.people`, `.numgrid` 등).
`gap+배경` 그리드에는 **`auto-fill` 이 아니라 `auto-fit`** 을 쓰세요. `auto-fill` 은
빈 트랙을 남겨서 검은 배경이 마지막 칸 옆에 판때기로 드러납니다.

> 다크모드 토글이 있었지만 클라이언트 요청으로 삭제했습니다. `--bg`/`--fg` 만
> 뒤집으면 되도록 설계는 남아 있으니, 필요하면 `html[data-inv]` 규칙 하나로 복구 가능합니다.
>
> 그 잔재로 `:root` 에 **`color-scheme:light`** 를 못 박아 두었습니다. 이게 없으면
> OS/브라우저가 다크 모드일 때 UA 가 폼 컨트롤에 자기 어두운 캔버스를 칠해
> **글쓰기 칸이 검게** 나옵니다. `input,textarea,select` 의 `background` 도
> `transparent` 가 아니라 `var(--bg)` 로 명시되어 있습니다. 둘 다 지우지 마세요.

**본문(`.art`) 안에서는 자유 색이 허용됩니다** — 유일한 예외이고, §8 #36 에서
뒤집힌 결정입니다. 팔레트 토큰 자체는 그대로이니 UI·레이아웃에는 쓰지 마세요.

**커서도 흑백입니다.** OS 화살표는 흰 몸통에 검은 테두리라 흰 종이 위에서 구멍처럼
보입니다. 그래서 화살표·손·글자 세 가지를 뒤집어(검은 몸통 + 흰 테두리) SVG 데이터
URI 로 넣었습니다. **흰 복사본을 굵은 흰 선으로 먼저 깔고 그 위에 검은 도형을 얹는**
방식입니다 — `paint-order` 나 도형별 stroke 로는 겹치는 부분에 흰 이음매가 생깁니다.
`url()` 뒤의 두 숫자는 핫스팟(실제로 가리키는 픽셀)이고, 맨 뒤 키워드는 이미지가
거부됐을 때의 폴백입니다. 지우지 마세요.

### 4-2. 서체

| 역할 | 폰트 | 라이선스 | 비고 |
|---|---|---|---|
| 제목·로고·네비 | **Anton** | SIL OFL 1.1 | Compacta 계열 초압축 헤비 산세리프 |
| 본문·라벨 | **SUIT Variable** | SIL OFL 1.1 | 한글/영문. 400–700 구간만 인스턴싱 |

둘 다 **base64로 index.html에 내장**되어 있습니다. CDN 의존 없음, 오프라인 동작,
기기 간 렌더링 동일. 대신 파일이 528KB입니다. 이건 의도된 트레이드오프입니다.

**⚠️ 한글 굵기 함정**
Anton에는 한글 글리프가 없습니다. "SLIT HOUSE 착공" 같은 제목에서 한글만 SUIT로
폴백되면서 얇게 떨어져 나옵니다. 그래서 제목류 셀렉터에

```css
font-variation-settings:'wght' 680
```

를 걸어뒀습니다. 가변 폰트인 SUIT에만 적용되고 정적 폰트인 Anton은 무시하므로
한글만 정확히 굵어집니다. **새 제목 스타일을 추가하면 이 셀렉터 목록에도 추가하세요.**
(index.html에서 `font-variation-settings` 로 검색)

### 4-3. 타이포 스케일
전부 `clamp()` 유동형입니다. 미디어쿼리로 폰트 크기를 바꾸지 마세요.

```css
.h-xl  clamp(44px, 11.5vw, 168px)
.h-l   clamp(30px,  6vw,    74px)
.h-m   clamp(20px,  2.9vw,  38px)
본문    clamp(13px,  1.02vw, 15px)
라벨    10~11.5px, letter-spacing .06~.12em, 대문자
```

### 4-4. 여백
```css
--pad:    clamp(16px, 3.2vw, 44px)    /* 모든 좌우 여백의 기준 */
--head-h: clamp(74px, 10.4vw, 108px)  /* 고정 헤더 높이 */
```
좌우 패딩은 **항상 `var(--pad)`** 를 쓰세요. 숫자를 직접 넣으면 정렬이 깨집니다.

### 4-5. 톤
- 라벨·메타데이터는 **영문 대문자 + 넓은 자간**. 도면의 주기(註記) 같은 인상
- 본문은 한국어, 건조하고 단정한 문어체. 감탄사·수식어 최소
- 애니메이션은 `--ease: cubic-bezier(.22,.61,.36,1)`. 튀지 않게
- 호버는 대부분 **흑백 반전** (`background:var(--fg); color:var(--bg)`)

---

## 5. 핵심 기능 구현

### 5-1. 슬롯머신 로고 — `_______BY X`

사이트 정체성의 핵심입니다. 함부로 바꾸지 마세요.

**개념**: 사무소 이름의 앞자리를 비워뒀습니다. 로고를 누르면 그 빈칸이 슬롯머신처럼
돌면서 우리가 다룰 수 있는 디자인 영역 단어에 멈춥니다. `LANDSCAPE BY X`, `CHAIR BY X`.

**구조** (이 분리가 중요합니다):
```html
<span id="slotwrap">        <!-- 위치 기준 -->
  <span id="slot">          <!-- overflow:hidden, 회전 중 blur가 걸리는 대상 -->
    <span id="reel">…</span><!-- translateY 로 움직이는 릴 -->
  </span>
  <span id="rule"></span>   <!-- 밑줄. slot 바깥이라 절대 안 움직이고 안 흐려짐 -->
</span>
```
밑줄이 `#slot` 밖에 있는 게 핵심입니다. 안에 넣으면 릴과 같이 움직이고 blur도 먹습니다.

**단어 풀** — 저장공간을 거의 안 씁니다.
- 착지 단어: `LANDING` 배열 (실제 의미 있는 디자인 영역 어휘 약 150개)
- 회전 중 스쳐가는 단어: `PRE`(30) + `ROOT`(60) + `SUF`(30) 조각을 `makeWord()` 가
  즉석 조합 → 수만 가지. 어차피 블러 처리되어 안 읽힙니다.

**애니메이션** — 42칸 릴, 2,700ms, WAAPI 4키프레임:
| 구간 | 이징 | 효과 |
|---|---|---|
| 0 → 34% | `cubic-bezier(.42,0,.72,.42)` | 가속, 거의 등속 |
| 34% → 93% | `cubic-bezier(.08,.55,.22,1)` | 감속 |
| 93% → 100% | 살짝 오버슛 후 복귀 | 물리적인 정지감 |

블러는 CSS 클래스 `.sp1`(1.6px) → `.sp2`(0.7px) → `.sp3`(0) 으로 3단 감쇠.

**함정**: `fill:'forwards'` 애니메이션은 끝나도 인라인 transform을 계속 덮어씁니다.
`onfinish` 에서 반드시 `setStatic()` **후에** `anim.cancel()` 을 호출해야 합니다.
순서를 바꾸면 로고가 사라집니다. (실제로 겪은 버그)

**긴 단어**: `fitItems()` 가 슬롯 너비를 넘는 단어에 `scaleX()` 를 걸어 줄입니다.
덕분에 단어 길이와 무관하게 `BY X` 위치가 절대 안 흔들립니다.

### 5-2. 절차적 흑백 아트 — `svgArt(seed)`

사진이 없는 항목의 자리를 채웁니다. 프로젝트 코드를 씨앗으로 SVG를 그립니다.
같은 코드 → 항상 같은 그림. 6가지 변형: 직교 평면 / 해칭 / 동심원 / 지층 / 매스 / 도트매트릭스.

`mulberry32` PRNG + FNV-1a 해시로 결정론적입니다. `Math.random()` 을 쓰면 안 됩니다.

`artOf(record)` 가 `record.img` 가 있으면 그걸, 없으면 생성 아트를 돌려줍니다.
**실사진을 올리면 자동으로 대체됩니다.** 회색 박스가 절대 안 보이게 하려는 장치입니다.

### 5-3. 프로젝트 2뷰

| 뷰 | 성격 | 구현 |
|---|---|---|
| GRID | 보여주기 위한 화면 | `.grid` 카드, 썸네일 기본 `grayscale(1)` → 호버 시 컬러 |
| INDEX | 확인하기 위한 화면 | `table.idx`, 열 제목 클릭 정렬, 행 클릭 이동 |

"표는 가장 압축된 형태의 도면"이라는 게 이 사무소의 관점입니다. INDEX는 부록이 아니라
본편으로 취급합니다. 유형 필터는 있었지만 클라이언트 요청으로 삭제했습니다.

### 5-4. ADMIN

`#/admin` (또는 `/admin`). 사이트 어디에도 링크가 없습니다 — 주소를 아는 사람만 들어옵니다.

- 탭: PROJECT / NEWS / BLOG / ABOUT / CONTACT
- `AD.draft` 에 편집 중인 레코드 사본을 두고, 입력할 때마다 갱신 (**재렌더 안 함** — 포커스 유지)
- 이미지 추가·삭제 등 구조가 바뀔 때만 `refreshAdmin()` 으로 다시 그림 (스크롤 위치 보존)
- 이미지: 드래그&드롭 또는 클릭 → `shrink()` → `DB.upload()` → Storage 공개 URL
- ABOUT/CONTACT 탭은 `settings` 표를 씁니다. 목록형 항목은 `키 | 값` 한 줄에 하나

**연결 상태 안내**는 SETUP 탭이 아니라 ADMIN 상단의 경고 상자로 보여줍니다.
데모 모드거나 `settings` 표가 없을 때만 나타나고, `hintFor()` 가 한국어 해결책을 붙입니다.
SQL 전문은 `배포-가이드.md` 3-2 에 있습니다 (탭 하나를 유지할 만한 내용이 아니라고 판단).

### 5-5. 페이지 내 편집 버튼

로그인(`session`)한 동안에만 `render()` 가 `editBar(page, arg)` 를 페이지 맨 위에 끼웁니다.
방문자에게는 아예 렌더되지 않습니다.

- 버튼 → `gotoAdmin('one:projects:<id>')` 같은 문자열 하나로 탭·드래프트를 세팅하고 `#/admin` 으로 이동
- `AD.back` 에 원래 해시를 담아두고, 저장·취소 후 `leaveEditor()` 가 그 페이지로 되돌림
- ADMIN 바에도 `← 사이트로` 버튼이 생김

### 5-6. 블로그 에디터 — 한 칸에 쭉 쓰기

저장 형태는 예전 그대로 **블록 배열**입니다. 공개 뷰가 그걸 읽기 때문에 바꾸지 않았습니다.
바뀐 건 **입력 방식**뿐입니다. 블록을 하나씩 추가하고 ↑↓로 옮기던 UI를 걷어내고,
텍스트 한 칸에 그냥 쓰게 했습니다.

`bodyToText()` / `textToBody()` 가 양방향 변환을 합니다.

**블록 표기**

| 입력 | 블록 |
|---|---|
| 빈 줄로 구분된 덩어리 | `{t:'p'}` |
| `## 제목` | `{t:'h'}` |
| `> 문장` | `{t:'q'}` |
| `![캡션](주소)` | `{t:'img', v, c}` |
| `![](a)` 줄이 **연달아** 여러 개 | `{t:'gal', items:[{v,c}…]}` — 스와이프 묶음 |
| 모든 줄이 `- ` 로 시작 | `{t:'ul', items:[…]}` |
| 모든 줄이 `1. ` 로 시작 | `{t:'ol', items:[…]}` |
| 한 줄에 `---` | `{t:'hr'}` |
| 맨 앞 `[가운데]` / `[오른쪽]` | `b.a = 'c' \| 'r'` |

빈 줄 없이 이어진 이미지 줄이 곧 묶음이라는 게 핵심입니다. **새 문법이 없습니다** —
사진을 여러 장 한 번에 넣으면 자동으로 묶음이 됩니다.

`textToBody()` 는 파싱한 뒤 **종류·정렬이 같은 이웃 목록 블록을 합칩니다**
(`textToBlocks()` 가 파싱, `textToBody()` 가 병합). 항목 사이에 빈 줄을 넣고 쓰는 것이
자연스러운데, 그대로 두면 `<ol>` 이 매번 1부터 다시 시작하기 때문입니다.

**인라인 표기** — `rich()` 가 렌더 시점에 해석합니다. 블록을 차지하지 않습니다.

| 입력 | 결과 |
|---|---|
| `**굵게**` | `<strong>` (SUIT wght 700) |
| `*기울임*` | `<em>` |
| `__밑줄__` | `<u>` |
| `~~취소선~~` | `<s>` |
| `==강조==` | `<mark>` — 흑백 반전 |
| `((흐리게))` | `--mute` |
| `@@제목체@@` | Anton(display) |
| `` `코드` `` | `<code>` |
| `[글](주소)` | `<a>` — http/https/mailto/tel/#/ 만 허용, 그 외는 `#` |
| `{글자색:#e02b2b\|글}` | `<span style="color:…">` |
| `{배경색:#ffe600\|글}` | `<span class="tx-bg" style="background:…;color:…">` |

> **보안**: `rich()` 는 `esc()` 를 **먼저** 돌린 뒤 위 표기만 태그로 바꿉니다.
> 작성자가 무엇을 쓰든 스스로 마크업이 될 수 없습니다. `javascript:` 링크는 무력화됩니다.
> 이 순서를 절대 뒤집지 마세요.
>
> **색 값도 마찬가지입니다.** `safeColor()` 를 통과한 3/6자리 hex 만 style 에 들어가고,
> 통과하지 못하면 표기가 통째로 사라져 글자만 남습니다. 이 검사를 느슨하게 바꾸면
> `style` 속성이 색 이외의 것을 실을 수 있게 됩니다. 정규식을 넓히지 마세요.

색 선택은 원래 넣지 않기로 했던 기능인데(§8 #33) **2026-08-22 클라이언트 요청으로
뒤집혔습니다**(#36). 열린 범위는 **본문 안뿐**이고 팔레트 토큰은 그대로입니다.
배경색은 `inkOn()` 이 명도로 흑/백 글자를 정하므로 어두운 배경에 검은 글자가 남는
조합이 나오지 않습니다.

툴바의 `글자색 ▾` / `배경색 ▾` 은 `colorPicker()` 가 그린 스와치 판을 열고 닫습니다.
`SWATCHES` 12색 + `<input type="color">`. 판을 여닫을 때 **재렌더하지 않습니다**
(`hidden` 속성만 토글) — 재렌더하면 커서 위치가 날아가서 선택이 사라집니다.
`색 지우기`(`clearColor()`)는 선택 영역, 선택이 없으면 커서가 있는 블록의 색 표기를 뺍니다.

이미지는 `＋ 이미지` 버튼이나 **텍스트 칸에 드래그&드롭** → 업로드 후 `insertAtCaret()` 이
커서 자리에 표기를 넣습니다. 툴바는 `wrapSelection()`(선택 감싸기),
`prefixBlock()`(커서가 있는 블록에만 토큰 붙이기), `listBlock()`(블록의 모든 줄에 마커)를
씁니다. 뒤의 둘은 `blockAt()` / `replaceBlock()` 위에 얹혀 있습니다.
`wrapSelection()` 은 `'양쪽'`, `'앞|뒤'`, `['앞','뒤']` 세 형태를 받습니다 —
색 표기는 여는 쪽에 `|` 가 들어 있어 배열 형태가 필요합니다.
**전부 재렌더를 하지 않습니다** — 하면 커서와 포커스가 날아갑니다.
`미리보기` 를 켜면 `syncBody()` 가 공개 뷰와 **같은 `postBodyHTML()`** 로 옆 칸을 갱신합니다.

기존 6편 시드 글로 왕복 변환이 무손실임을 확인했습니다.

### 5-7. 이미지 자동 축소 — `shrink(file)`

`DB.upload()` 가 항상 먼저 부릅니다. 긴 변 **2560px**(QHD 기준 전체 폭이 최대 슬롯),
**WebP q0.82**. 6000×4000 / 2.9MB 사진이 2560×1707 / 326KB 가 됩니다(약 1/10).

- GIF·SVG는 통과 (리샘플하면 깨짐)
- 결과가 원본보다 크면 원본을 그대로 씀
- WebP 미지원 브라우저는 JPEG로 폴백하며 알파가 없으므로 흰 배경을 먼저 칠함
- `file.shrunk` 플래그로 멱등 — 호출부에서 미리 줄여도 두 번 인코딩되지 않음

---

### 5-8. 포스트 커버 + 스와이프 묶음

**커버**: `.post-cover` 가 `position:sticky; top:var(--head-h)` 로 한 화면을 채운 채 고정되고,
`.post-sheet`(불투명 배경 + `z-index:1`)가 그 위로 미끄러져 올라옵니다. 스크립트 없음.
`100svh` 앞에 `100vh` 폴백 줄을 둡니다.

**본문 칼럼**: `.art` 는 `max-width:78ch; margin-inline:auto` 로 가운데 정렬입니다.
좌우는 여백입니다.

**스와이프 묶음**: CSS `scroll-snap-type:x mandatory` 한 줄이 터치 스와이프를 다 합니다.
화살표·점·카운터만 `bindGalleries()` 가 붙입니다.

> **함정**: 화살표에 `scrollTo({behavior:'smooth'})` 를 쓰면 안 됩니다. 애니메이션이
> 불가능한 환경(숨은 탭, reduced-motion)에서 조용히 무시되는 데다, **떠 있는 스무스
> 스크롤이 뒤이은 `scrollLeft` 대입까지 삼켜버려** 슬라이드가 아예 안 넘어갑니다.
> 그래서 `step()` 은 rAF로 직접 트윈하고, 애니메이션이 불가능하면 즉시 대입합니다.

---

### 5-9. 사람과 참여자 — 줄 하나가 한 사람

ABOUT 의 PEOPLE 은 `settings.about.people` 텍스트이고, **한 줄이 한 사람**입니다.

```
이름 | 직책 | 이력 | 사진주소
PARK | Principal | 서울대 건축학과 / 前 OMA Rotterdam | https://…/a.webp
```

- `cells()` 가 줄을 `|` 로 전부 쪼갭니다. `pairs()` 는 **첫 `|` 에서만** 자르므로
  PEOPLE 에는 쓰면 안 됩니다 (FACTS·RECORDS·프로젝트 참여자는 `pairs()`)
- 이력의 ` / ` 는 공개 뷰에서 줄바꿈이 됩니다
- 사진은 ADMIN 의 타일(`peopleShots()`)에서 넣고, `setPersonPhoto()` 가 **그 사람 줄의
  4번째 칸**에 씁니다. 위치로 짝지은 별도 배열을 쓰지 않은 이유는 §8 #38
- 타일은 이름 칸을 **떠날 때**(`onchange`) 다시 그립니다. 타이핑 중에 그리면
  포커스가 날아갑니다
- 사진이 없으면 `svgArt('person-'+이름+i)` 도형이 들어갑니다. 공개 뷰와 ADMIN 타일이
  **같은 시드**를 써야 미리보기가 실제와 같습니다

프로젝트의 `people` 은 다른 것입니다 — `역할 | 이름` 을 `pairs()` 로 읽어
상세 페이지 스펙 표(`.specs dl`) 뒤에 붙입니다. `|` 가 없는 줄은 `Credit` 으로 들어갑니다.

### 5-10. 소식의 사진과 링크

- `news.img` 가 있으면 행 왼쪽에 132px 썸네일이 붙습니다
- `news.url` 이 있으면 행 자체가 `<a target="_blank">` 가 됩니다. `SAFE_HREF()` 를
  통과하지 못하는 주소는 링크가 되지 않고 그냥 행으로 남습니다.
  주소 아래에 `hostOf()` 로 뽑은 도메인을 적어 어디로 가는지 미리 보입니다
- **한 건이라도 사진이 있으면 모든 행이 사진 칸을 유지합니다**(`.th.empty`).
  안 그러면 제목의 왼쪽 끝이 행마다 들쭉날쭉해집니다

### 5-11. CONTACT — 복사와 지도

- 메일 주소는 `mailto:` 가 아니라 **클립보드 복사** 버튼입니다(§8 #39).
  `navigator.clipboard` → 실패하면 숨은 textarea + `execCommand('copy')` 순으로
  시도합니다. 첫 번째는 보안 컨텍스트와 **포커스된 문서**를 요구해서 조용히 실패할 수
  있으므로 폴백을 지우지 마세요
- 지도는 약도 이미지가 있으면 **그 이미지**, 없으면 구글 키리스 임베드입니다.
  검색어는 `mapq` → 없으면 `address` (`mapQuery()`)
- **이것이 유일하게 늘어난 외부 로드입니다.** 되돌리려면 약도 이미지를 올리면 됩니다
  (코드 변경 없이 이미지가 지도를 대체합니다)
- **다크 그레이 스타일은 CSS 필터입니다.** 키리스 임베드는 style 파라미터를 받지 않으므로
  `grayscale(1) invert(1)` 로 만듭니다 — 밝은 지도를 뒤집으면 어두운 지도에 밝은 라벨이
  됩니다. 호버는 밝은 지도로 되돌아가지 **않습니다**(어두운 화면에서 흰색이 번쩍임).
  로딩 중 `.map` 배경이 흰색이면 그 순간만 밝게 새므로 `#141414` 를 깔아 둡니다
- 라벨 언어는 `hl=en`. 한국 장소는 구글이 영문 아래 국문을 함께 붙이는데, 이건 구글의
  데이터 표시 방식이라 파라미터로 끌 수 없습니다
- 주소가 실제로 존재하지 않으면 좌상단 장소 카드가 계속 도는 상태로 남고, 반전 때문에
  **검은 상자**처럼 보입니다. 코드 문제가 아니라 주소 문제입니다 (§9 참고)

### 5-12. 비밀글

`posts.pub === false` 인 글은 사이트에 없는 것처럼 굽니다.

- `livePosts()` 가 목록을 거릅니다. 로그인 중이면 거르지 않고 `.lock` 표시를 붙입니다
- `viewPost()` 는 **주소를 직접 쳐도** `Not Found` 를 돌려줍니다. 목록에서 빼는 것만으로는
  숨긴 것이 아닙니다
- **`pub` 이 없으면 공개로 봅니다** (`p.pub === false` 만 숨김). 기존 글, 그리고 아직
  칸이 없는 데이터베이스가 통째로 사라지면 안 됩니다
- 편집 화면의 `.pubbar` 는 비밀글일 때 흑백이 반전됩니다. 저장 토스트도 상태를 말합니다
- 새 글은 `blank()` 에서 `pub:false` 로 시작합니다 (§8 #51)

### 5-13. 슬롯머신 연장

`spin()` 은 돌고 있을 때 불려도 무시하지 않고 **이어 붙입니다**.

- `reelY()` 로 현재 위치를 읽고 → 애니메이션을 `cancel()` → 그 위치를 인라인 transform 에
  박아두고 → 릴 끝에 26개를 더 붙인 뒤 → 거기서부터 다시 애니메이트
- **위치를 먼저 읽어야 합니다.** `cancel()` 이 transform 을 되돌립니다
- 길이는 남은 거리에 비례해 잡되 1.5~4.6초로 자릅니다. 그래야 연장이 급해지지 않고
  거의 끝난 릴이 굼뜨지 않습니다
- 항목이 `REEL_MAX`(300)에 닿으면 더 붙이지 않고 마지막 항목의 단어만 바꿔 답니다
- 착지는 `onfinish` **와** `dur + 260ms` 타이머 두 갈래입니다. 숨은 탭은 애니메이션을
  `finished` 까지 돌려놓고도 이벤트를 안 줄 수 있어, 그때 릴이 영원히 중간에 섭니다.
  두 경로 모두 `reelAnim !== anim` 이면 아무 것도 하지 않습니다

### 5-14. 로고 간격 — `.optical`

`#brand .fixed` 와 ABOUT `.brandline` 의 `&nbsp;BY&nbsp;X` 앞 공백은 **실제 공백 문자**라
`BY X` 의 공백과 정의상 같습니다(§8 #28). 그런데도 좁아 보입니다 — `Y` 가 베이스라인
쪽에서 열려 있어 뒤 간격에 여백을 빌려주기 때문입니다.

캔버스 픽셀 스캔 실측(57px, "BAR BY X"): **R→B 17px / Y→X 14px**. 물리적으로는 앞이
더 넓은데도 뒤가 더 넓게 읽힙니다. 그래서 `.optical{padding-left:.05em}` 만 더했습니다.
**이건 착시 보정값이지 공백을 대신하는 값이 아닙니다.** 키우지도, 지우지도 마세요.

## 6. Supabase

### 6-1. 현재 설정 (index.html 17~21줄)
```js
window.BYX_CONFIG = {
  url: 'https://uvkcpzmjcpcdaeozvysv.supabase.co',
  key: 'sb_publishable_nD-EQ8iq0PhcOwk_AB8fXw_v0kdHBsP',
  bucket: 'media'
};
```
publishable(구 anon) 키는 **공개되어도 되는 키**입니다. RLS가 읽기만 허용합니다.
`service_role` / `secret` 키는 절대 파일에 넣지 마세요.

> Supabase가 `anon` JWT를 `sb_publishable_...` 형식으로 교체하는 중입니다.
> supabase-js는 둘 다 받습니다.

### 6-2. 스키마
```sql
projects(id uuid pk, code, name, client, type, loc, year int, month int, status,
         area, role, img, shots jsonb, descr, people, sort int, created_at)
news    (id uuid pk, d, t, k, x, img, url, sort int, created_at)
posts   (id uuid pk, slug unique, d, k, t, x, body jsonb, pub bool, sort int, created_at)
settings(k text pk, v jsonb, updated_at)
```
- 열 이름이 짧은 건 초기 시드 데이터 구조를 그대로 옮겼기 때문입니다
  (`d`=날짜, `t`=제목, `k`=분류, `x`=요약)
- `descr` 은 `desc` 가 SQL 예약어라서 그렇게 지었습니다
- `sort` 가 클수록 위. 정렬은 `order('sort', {ascending:false})`.
  **`sort` 는 이제 사람이 넣지 않습니다.** 저장할 때 날짜에서 계산합니다 —
  프로젝트는 `ymNum()` (`202607`), 소식·글은 `dnum(d)` (`20260728`).
  열을 남겨둔 건 Supabase 정렬을 그대로 쓰기 위해서입니다
- `settings` 는 `k` 가 `'about'` / `'contact'` 두 행뿐입니다. 없어도 사이트는 돕니다
- `posts.pub` 은 `default true` 로 만듭니다. 이미 올라간 글이 마이그레이션 한 번에
  전부 사라지면 안 됩니다. 새 글만 `blank()` 에서 `pub:false` 로 시작합니다
- `projects.people` / `news.img` / `news.url` / `posts.pub` 은 **나중에 붙은 칸**이라
  아직 없을 수 있습니다. `DB.refresh()` 가 첫 행의 키로 존재를 판정해 `DB.cols` 에 담고,
  `DB.save()` 가 없는 칸만 payload 에서 뺍니다. 편집 화면에는 `miss()` 안내 상자가
  뜹니다. **이 폴백을 지우면 SQL 을 실행하지 않은 사이트에서 저장 전체가 실패합니다**
- 표에 **행이 하나도 없으면 판정할 근거가 없어 "있다"로 둡니다.** 그 상태로 저장하면
  Supabase 가 에러를 내고, `hintFor()` 가 어느 SQL 을 실행할지 알려줍니다

### 6-3. 보안
- 3개 표 모두 RLS 켜짐. `select` 는 누구나, 그 외는 `auth.role() = 'authenticated'`
- **Authentication → Email → "Allow new users to sign up" 반드시 OFF**
  이걸 켜두면 아무나 가입해서 글을 고칠 수 있습니다. 정책의 전제가 무너집니다
- 관리자 계정은 Supabase 대시보드에서 수동 생성 (Auto Confirm User 체크)
- Storage `media` 버킷은 Public

---

## 7. 배포

```
GitHub(byx-website) --push--> Vercel --auto deploy--> https://…
```
- 빌드 설정 없음. Framework Preset `Other`, Build Command 비움
- `vercel.json` 이 `/admin` → `/index.html` rewrite (없으면 404)
- 커밋할 때마다 30초 내 자동 재배포
- 상세 절차는 `배포-가이드.md` (클라이언트용, 비개발자 기준으로 작성)

---

## 8. 의사결정 기록

시간순. **"왜 이렇게 되어 있지?"** 싶을 때 여기를 보세요.

| # | 결정 | 이유 |
|---|---|---|
| 1 | 단일 HTML 파일 | 클라이언트가 비개발자. 빌드·다중 파일은 유지보수 불가 |
| 2 | Anton으로 Compacta 대체 | Compacta는 유료. Anton이 OFL이면서 초압축 헤비로 성격이 가장 가까움 |
| 3 | 폰트 base64 내장 | PC/모바일 렌더링 일치 요구. CDN 장애·차단에도 안전 |
| 4 | 슬롯 단어 조합 생성 | "1000개 단어를 저장공간 안 쓰고" 요구 → 형태소 105개로 수만 조합 |
| 5 | 절차적 SVG 아트 | 사진 없는 상태에서도 완성된 화면. 회색 박스 금지 |
| 6 | 데모 모드 폴백 | 설정 전에도 전 기능 시험 가능. 로컬 확인 수단 |
| 7 | Supabase 채택 | GitHub 방식(Decap)은 진입장벽이 너무 높다고 판단. 즉시 반영이 필요 |
| 8 | 홈 = 프로젝트 페이지 | 히어로·마퀴·뉴스 섹션 제거. 작품이 먼저 보여야 함 |
| 9 | 밑줄을 문자에서 도형으로 | `_______` 반복은 큰 크기에서 점선처럼 끊겨 보임 → 이어진 선으로 |
| 10 | 밑줄을 릴 바깥으로 분리 | "슬롯이 움직여도 밑줄은 고정" 요구 |
| 11 | 유형 필터 삭제 | 프로젝트 18개 규모에선 불필요한 장치 |
| 12 | INQUIRY 폼 삭제 | 백엔드 없이 mailto는 어중간. 메일 주소 직접 노출이 정직 |
| 13 | 다크모드 삭제 | 흑백 사이트에서 반전은 개념적으로 중복 |
| 14 | ADMIN 링크 제거 | 주소를 아는 사람만. 푸터가 깨끗해짐 |
| 15 | 무한 스크롤 | 프로젝트가 늘어날 것을 전제. 초기 로드 경량화 |
| 16 | 썸네일 흑백→호버 컬러 | 그리드 전체의 톤 통일. 현재 생성 아트에선 효과가 안 보임(실사진 대비용) |
| 17 | 본문 폰트 SUIT | 한글 렌더링 품질. 클라이언트 지정 |
| 18 | 설정 블록을 파일 맨 위로 | GitHub 웹 편집기에서 62KB base64 뒤를 뒤지게 할 순 없음 |
| 19 | 페이지 상단 제목·설명·개수 삭제 | 메뉴에 이미 있는 정보. 콘텐츠가 바로 시작 |
| 20 | ADMIN SETUP 탭 삭제 | 최초 1회만 보는 화면이 상시 탭을 차지. SQL은 배포 가이드에 있고, 연결 문제는 필요할 때만 경고 상자로 |
| 21 | 페이지별 편집 버튼 | ADMIN 목록에서 항목을 다시 찾는 왕복이 불필요. 보고 있는 것을 바로 고침 |
| 22 | ABOUT/CONTACT를 `settings` 표로 | 하드코딩 더미를 클라이언트가 못 고치던 문제. §9의 미결정 항목을 확정 |
| 23 | 정렬값(sort) 입력란 삭제 | 날짜가 이미 순서를 결정함. 두 곳에 같은 뜻을 적게 하면 어긋남 |
| 24 | 블로그 블록 UI → 한 칸 쓰기 | 문단마다 블록을 만들고 ↑↓로 옮기는 건 글쓰기 리듬을 끊음. 저장 형태는 그대로 |
| 25 | 업로드 시 자동 축소 | 카메라 원본 8MB를 그대로 올리면 무료 플랜이 금방 참. 2560px/WebP면 육안 차이 없음 |
| 26 | 프로젝트 코드 `BYX-` 접두 제거 | 사무소 사이트에서 사무소 약자를 매번 반복할 이유가 없음 |
| 27 | 연도 → 연월 | 같은 해 프로젝트의 선후가 안 보였음 |
| 28 | 로고 앞뒤 간격을 공백 문자로 | `padding-left` 로 흉내 내던 값이 `BY X` 의 공백과 미세하게 달랐음. 실제 공백을 쓰면 정의상 같아짐 |
| 29 | ABOUT 머리글을 한 줄로 | 두 줄로 접히면 로고가 아니라 문단처럼 읽힘 |
| 30 | 포스트 커버 + 고정 스크롤 | the-edit.co.kr 참고. 글의 진입에 호흡을 준다 |
| 31 | 본문 칼럼 가운데 정렬 | 긴 글은 좌측 정렬 전면폭보다 가운데 좁은 칼럼이 읽힌다 |
| 32 | 인라인 서식을 마크다운식 표기로 | contenteditable 리치 에디터는 붙여넣기 오염·정리 코드가 본체보다 커짐. 표기는 저장이 평문이라 안전하고, 툴바가 대신 넣어줌 |
| 33 | ~~색 선택 기능은 넣지 않음~~ | **2026-08-22 뒤집힘 → #36.** 원래 이유: 팔레트가 네 토큰(§4-1), 임의 색을 열면 시스템이 무너짐 |
| 34 | 연속 이미지 줄 = 스와이프 묶음 | 묶음 전용 문법을 만들지 않기 위해. 여러 장을 한 번에 넣으면 그냥 묶음이 됨 |
| 35 | 캐러셀은 scroll-snap, 라이브러리 없음 | 참고 사이트는 Swiper.js. CSS 한 줄로 터치 스와이프가 되므로 의존성을 늘릴 이유가 없음 |
| 36 | 본문에 한해 자유 색 허용 (#33 뒤집음) | 클라이언트 직접 요청. **본문 안에서만** 열었고 팔레트 토큰은 그대로. 값은 `safeColor()` 가 3/6자리 hex 만 통과시켜 style 속성이 색 외의 것이 될 수 없음. 배경색은 `inkOn()` 이 명도로 흑/백 글자를 정해 안 보이는 조합이 안 나옴 |
| 37 | 참여자·소식 사진·소식 링크는 "없어도 되는 칸" | 열 추가에 SQL 이 필요한데 클라이언트가 실행 전이어도 나머지는 저장돼야 함. `DB.cols` 가 첫 행의 키로 존재를 판정하고, 없으면 payload 에서 그 필드만 뺌 (`settings` 표 설계와 같은 태도) |
| 38 | PEOPLE 사진·이력을 같은 줄의 칸으로 | 위치로 짝지은 별도 목록은 이름을 지우거나 옮기는 순간 어긋남. 한 줄 = 한 사람이면 어긋날 수가 없음 |
| 39 | 이메일은 mailto 대신 클립보드 복사 | 설정한 적 없는 메일 클라이언트가 열리는 것이 주소를 건네받는 것보다 불편함. 클라이언트 요청 |
| 40 | 지도는 구글 키리스 임베드 | 네이버는 키+스크립트가 필요. 구글 `?q=…&output=embed` 는 키도 스크립트도 없이 iframe 하나. **다만 외부 로드가 하나 늘었음** — 약도 이미지를 올리면 지도 대신 그 그림이 나가므로 되돌릴 수 있음 |
| 41 | 썸네일의 유형 배지 삭제 | 유형은 INDEX 뷰와 프로젝트 페이지에 이미 있음. 사진 위에서는 작품에 붙은 라벨처럼 읽힘 |
| 42 | 메뉴 이름만 WORKS, 경로는 `project` | 이름은 부르기 나름이지만 주소가 바뀌면 이미 나간 링크가 깨짐 |
| 43 | 로고 `.optical` 보정값 | #28 로 공백은 실제로 같아졌는데도 좁아 보임. Y 가 베이스라인에서 열려 있어 뒤쪽 간격에 여백을 빌려주기 때문. 픽셀 스캔 실측 R→B 17px / Y→X 14px 인데 후자가 더 넓게 읽힘 → `.05em` 만 더함 |
| 44 | 커서를 흑백 반전 이미지로 | 클라이언트 요청. OS 커서는 색을 지정할 수 없어 이미지 공급이 유일한 방법. 흰 밑깔기 + 검은 도형 2중 렌더 — 도형별 stroke 는 겹치는 곳에 흰 이음매가 생김 |
| 45 | 지도 다크 그레이는 CSS 필터 | 키리스 임베드는 style 파라미터가 없음. `grayscale(1) invert(1)` 이면 스크립트 한 줄 없이 어두운 지도 + 밝은 라벨이 나옴 |
| 46 | 커서 값을 `:root` 변수로 | 선택자 목록 하나로 깔았더니 `#brand`(id)·`table.idx tr`·`.cpick .sw` 처럼 이미 `cursor:pointer` 를 가진 더 강한 규칙이 이겨서 그 자리만 OS 커서로 돌아갔음. 변수는 특이도로 질 수 없음 |
| 47 | 슬롯머신은 재시작이 아니라 연장 | 돌던 것을 끊고 다시 시작하면 클릭이 리셋처럼 보임. 현재 위치를 읽어 뒤에 항목을 이어 붙이면 "계속 돈다"로 읽힘. 항목 300개에서 멈춤 |
| 48 | 프로젝트 기본 정렬을 `year` 로 | 기존 기본값 `code` 내림차순은 데모 코드가 연도로 시작해서 맞아 보였을 뿐. 실제 코드에는 날짜가 없음 |
| 49 | 글 날짜는 8자리 + 달력 | 자유 텍스트라 구두점을 틀릴 수 있었음. 저장 형태(`2026.06.02`)는 그대로 두고 **편집 화면만** 숫자로. 공개 화면과 기존 데이터가 안 바뀜 |
| 50 | 미리보기 기본 열림 | 표기법은 결과를 봐야 익혀짐. 닫는 버튼은 남겨둠 |
| 51 | 새 글은 비밀글로 시작 | "쓰고 나서 공개"가 블로그의 기본 흐름. 대신 저장 토스트가 비밀글임을 말해주고, 편집 화면 맨 줄이 검게 반전됨 |

### 되돌린 것들 (다시 제안하지 마세요)
- 다크모드 토글 — 삭제됨
- 프로젝트 유형 필터 칩 — 삭제됨
- CONTACT 문의 폼 — 삭제됨
- CONTACT 대문짝만한 이메일(`.bigmail`) — 삭제됨
- CONTACT 메일 3분할(general/career/press) — 대표 메일 하나로
- CONTACT VISIT 아래 INSTAGRAM / BEHANCE / LINKEDIN — 삭제됨 (푸터에 인스타 아이콘이 있음)
- 페이지 하단 CTA 블록(SELECTED WORKS / START A PROJECT) — 삭제됨
- ABOUT의 APPROACH / SERVICES / CLIENTS 섹션 — 삭제됨
- 푸터의 STUDIO / ADDRESS / CONTACT 4열 — 삭제됨 (내용은 ABOUT·CONTACT에 있음)
- ADMIN SETUP 탭 — 삭제됨
- 정렬값(sort) 입력란 — 삭제됨 (열은 남아 있지만 자동 계산)
- 블로그 블록 추가·이동 UI — 삭제됨
- 프로젝트 썸네일 위의 유형 배지(`.card .badge`) — 삭제됨
- CONTACT 이메일의 `mailto:` 링크 — 클립보드 복사로 대체됨

---

## 9. 남은 일 / 알려진 한계

### 해야 할 일
- [x] **Supabase 마이그레이션 SQL 실행** — 2026-08-18 완료.
      `projects.month` 열 존재, `settings` 표 생성·조회 정상, 비로그인 쓰기는 RLS로 차단(42501) 확인.
      단, `settings` 는 아직 0행이라 ABOUT/CONTACT 는 파일 기본값이 보이는 상태입니다
- [ ] **로그인 상태에서 실제 저장 확인** — 미검증. 계정이 없어 익명 읽기까지만 확인했습니다.
      ADMIN 로그인 → ABOUT 한 글자 수정 → 저장 → 새로고침으로 한 번 확인 필요
- [ ] **새 열 4개 SQL 실행** — `projects.people`, `news.img`, `news.url`,
      `posts.pub`(`default true`). 배포 가이드 3-2 의 "예전에 이미 이 단계를 한 적이
      있다면" 상자. 실행 전에도 사이트는 정상이고, 그 네 칸만 저장되지 않습니다 (§6-2).
      **비밀글은 이걸 해야 실제로 숨겨집니다** — 그 전에는 새로고침하면 공개로 돌아옵니다
- [ ] 실제 프로젝트 사진·텍스트 입력 (ADMIN에서)
- [ ] ABOUT/CONTACT 기본값이 여전히 지어낸 더미입니다. 이제 ADMIN에서 고칠 수 있으니
      실제 내용으로 교체 필요 (`DEFAULT_ABOUT` / `DEFAULT_CONTACT` 는 폴백일 뿐).
      PEOPLE 의 이력도 지어낸 값입니다
- [ ] CONTACT 주소가 더미라 지도가 엉뚱한 곳을 가리킵니다. 실제 주소 입력 필요
      (또는 `Map search` 칸에 정확한 검색어)
- [x] **favicon** — 2026-08-22 완료. `BYX_favicon.svg` 를 base64 로 `<link rel="icon">` 에 내장
- [ ] OG 이미지
- [ ] 도메인 연결
- [ ] 국문/영문 전환 필요 여부 결정

### 알려진 한계
- 검색엔진에 약합니다. 해시 라우팅 + 클라이언트 렌더 → 개별 프로젝트 페이지가 색인되지 않음.
  중요해지면 정적 생성이나 `<noscript>` 폴백을 검토
- 이미지는 업로드 시 2560px/WebP로 줄지만(§5-7), 표시 크기별 여러 벌을 만들지는 않음.
  모바일도 2560px 한 벌을 받습니다. 문제가 되면 Supabase Image Transformation 검토
- 접근성 미검증. 키보드 네비게이션, 스크린리더 테스트 안 함
- Supabase 무료 플랜은 자동 백업이 제한적. 분기별 CSV 수동 백업 권장
- 프로젝트 상세 이미지는 순서 변경 UI가 없음 (삭제 후 재업로드해야 함)

---

## 10. 작업 시 주의사항

### 절대 하지 말 것
- ❌ 빌드 도구·프레임워크·npm 도입
- ❌ 파일 분리 (`vercel.json` 외)
- ❌ `service_role` / `secret` 키를 파일에 넣기
- ❌ 데모 모드 폴백 제거
- ❌ `svgArt()` 에 `Math.random()` 사용 (결정론이 깨짐)
- ❌ 색상 추가 (흑백 + `--mute` + `--faint` 가 전부)

### 조심할 것
- 뷰 함수는 **문자열을 반환**합니다. 사용자 입력은 반드시 `esc()` 로 이스케이프
- `render()` 는 `#app` 전체를 갈아끼웁니다. 이벤트는 `bindPage()` 에서 다시 붙여야 합니다
- ADMIN에서 입력 중에는 재렌더하지 마세요 (포커스·커서 위치가 날아감).
  특히 블로그 글쓰기 칸은 커서 위치가 곧 이미지 삽입 위치입니다 — `insertAtCaret()` 만 쓰세요
- 새 제목 스타일에는 `font-variation-settings:'wght' 680` 셀렉터 추가
- 좌우 여백은 `var(--pad)`, 경계선은 `var(--hair)`
- Supabase 열 이름을 바꾸면 `SEED_*`, 뷰 함수, ADMIN 폼 세 군데를 같이 고쳐야 합니다
- `sort` 를 폼에 다시 넣지 마세요. 날짜에서 계산합니다 (§6-2)
- ABOUT/CONTACT에 항목을 추가하면 `DEFAULT_*` 와 `edSettings()` 와 뷰 함수 세 군데를 같이
- `rich()` 는 `esc()` 뒤에만 돕니다. 순서를 뒤집으면 그 자리가 XSS 구멍입니다
- 색 값은 `safeColor()` 를 통과한 hex 만 `style` 에 들어갑니다. 정규식을 넓히지 마세요
- 캐러셀 이동에 `behavior:'smooth'` 를 쓰지 마세요 (§5-8 함정)
- `:root` 의 `color-scheme:light` 와 폼 컨트롤의 `background:var(--bg)` 를 지우지 마세요.
  없으면 다크 모드 브라우저에서 글쓰기 칸이 검게 나옵니다 (§4-1)
- 전역 `cells()` 와 `svgArt()`/`viewProjects()` 안의 지역 `cols` 는 다른 것입니다.
  전역 헬퍼에 `cols` 라는 이름을 다시 쓰지 마세요
- 새 열을 추가하면 `DB.cols` 판정·`DB.save()` 의 제거·`miss()` 안내 세 곳을 같이 넣으세요.
  안 그러면 SQL 을 실행하지 않은 사이트에서 저장 전체가 실패합니다 (§6-2)
- `gap+검은 배경` 그리드는 `auto-fit`. `auto-fill` 은 빈 트랙에 검은 판을 남깁니다
- 메뉴 이름은 WORKS 지만 라우트·`data-p` 는 `project` 입니다. 주소를 바꾸지 마세요

### 테스트 방법
빌드가 없으므로 파일을 브라우저로 열면 끝입니다.
Playwright로 회귀 확인하는 스니펫:

```js
// 모든 라우트가 내용을 렌더하는지 + 콘솔 에러가 없는지
const { chromium } = require('playwright');
const U = 'file:///.../index.html';
const routes = ['#/','#/news','#/blog','#/about','#/contact','#/admin','#/project/2604'];
(async () => {
  const b = await chromium.launch(); const errs = [];
  for (const h of routes) {
    const p = await b.newPage();
    p.on('pageerror', e => errs.push(h + ': ' + e.message));
    await p.goto(U + h, { waitUntil:'domcontentloaded' });
    await p.waitForTimeout(1600);
    const len = await p.evaluate(() => document.querySelector('#app').innerText.length);
    if (len < 30) errs.push(h + ' empty');
    await p.close();
  }
  await b.close();
  console.log(errs.length ? errs.join('\n') : 'OK');
})();
```

확인 항목: 데스크톱(1440) / 모바일(390) 두 폭, 슬롯머신 회전·정지,
그리드 무한 스크롤, INDEX 정렬(Date 열이 연+월로 정렬되는지), ADMIN 저장,
로그인 상태에서 각 페이지의 편집바 노출·비로그인 시 미노출,
블로그 본문 텍스트 ↔ 블록 왕복 변환(`textToBody(bodyToText(x))`),
이미지 업로드 시 축소 여부(`shrink()` 반환 크기),
포스트 커버가 고정된 채 본문이 위로 덮는지, 스와이프 묶음의 화살표·점·카운터,
`rich()` 가 `<script>` 와 `javascript:` 를 무력화하는지.

> **캐시 주의**: 로컬 서버로 확인할 때 브라우저가 이전 `index.html` 을 붙잡고 있어
> "고쳤는데 그대로"인 것처럼 보이는 일이 잦습니다. 주소에 `?v=1` 같은 쿼리를 바꿔
> 붙이거나 강력 새로고침 하세요. (이것 때문에 멀쩡한 코드를 한참 디버깅한 적 있음)

> **자동화 탭 주의**: 확장으로 조종하는 탭은 `document.hidden === true` 라
> `requestAnimationFrame` 과 UA 스무스 스크롤이 아예 돌지 않고, `setTimeout` 도
> 1초로 클램프됩니다. 애니메이션이 "안 되는" 것처럼 보이면 먼저 `document.hidden` 을
> 확인하세요. 코드 문제가 아닐 수 있습니다.

> 로컬 확인은 `file://` 로도 되지만, 브라우저 확장으로 자동화할 때는 `file://` 이 막힙니다.
> 아무 정적 서버로 띄우고 `http://localhost:...` 로 여세요.
> Supabase에 손대지 않고 시험하려면 `BYX_CONFIG` 의 url/key 를 비운 사본을 쓰면 됩니다.

---

## 11. 콘텐츠 톤 가이드

새 문구를 쓸 일이 있으면 이 톤을 따라주세요.

- **건조하고 단정하게.** 감탄사, 과장, 마케팅 문구 금지
- **결정의 근거를 쓴다.** "아름다운 공간을 만듭니다" (X) / "선을 줄이고, 남은 선을 정확히 놓는다" (O)
- 라벨은 영문 대문자, 본문은 한국어
- 블로그는 설계 과정에서 반복해 부딪히는 질문을 짧게 정리하는 성격.
  기존 6편(`Why Black and White`, `Index as Drawing`, `The Blank Line`,
  `One Millimeter`, `Drawing with Code`, `Apartment, Again`)의 어조를 참고

예시 — 사무소의 3원칙 (ABOUT에서는 삭제했지만 톤의 기준으로 남깁니다):
1. 선을 줄인다 — 필요한 선만 남기고, 남은 선에만 두께를 준다
2. 재료가 색을 만든다 — 칠하지 않는다. 노출된 표면과 조도가 만드는 회색을 신뢰한다
3. 스케일을 가리지 않는다 — 대지 8,000㎡와 손잡이 40mm는 같은 방법으로 다뤄진다
