# 변경 이력

작업 로그. 최신이 위로 옵니다.

---

# 2026-08-22

클라이언트가 쓰면서 걸린 것들을 한 번에 걷어낸 날입니다. 요청이 대화 중에 계속
추가되어 13건이 되었고, 성격은 셋으로 나뉩니다 — **글쓰기 도구를 넓힌 것**(색·목록·
구분선·밑줄·코드), **손댈 수 없던 데이터를 열어준 것**(구성원 사진·이력, 프로젝트 참여자,
소식 사진·링크), **눈에 걸리던 것을 고친 것**(검은 입력창, 썸네일 배지, mailto, 커서,
로고 간격, 파비콘, 메뉴 이름).

두 가지 결정이 뒤집혔습니다. **본문 색 선택은 넣지 않는다**(§8 #33)가 클라이언트 직접
요청으로 열렸고, **외부 로드는 supabase-js 하나**라는 원칙이 지도 임베드로 하나 늘었습니다.
둘 다 뒤집은 이유와 되돌리는 방법을 `PROJECT.md` §8 #36·#40 에 남겼습니다.

시간을 가장 많이 쓴 건 원인이 코드가 아니었던 네 건입니다 (아래 **반증** 항목).
검은 입력창, 좁아 보이는 로고 간격, 지도의 검은 상자, 멈추지 않는 슬롯머신 —
전부 코드를 고쳐서 될 일이 아니었고, 실측으로 원인을 갈라낸 뒤에야 맞는 처방이 나왔습니다.

## [슬롯머신] 회전 시점

### 페이지 이동마다 회전
- **`render()` 에서 라우트가 바뀔 때만 `spin()`**
  - 변경 파일: `index.html` (`lastRoute`, `render()`)
  - 이유: 페이지를 옮길 때마다 단어가 새로 나오길 원함. 다만 `render()` 는 같은 페이지
    안에서도 불립니다(그리드/인덱스 전환, 열 정렬) — 그때까지 돌면 산만해집니다
  - `route = page + '/' + (arg||'')` 를 비교합니다. `lastRoute` 는 `null` 로 시작해
    **첫 렌더는 건너뜁니다** — 첫 회전은 부팅 코드가 폰트가 자리잡은 뒤(700ms) 돌립니다
  - 실측: 전 라우트 이동에서 `spinning===true` 진입 확인, 화면에 보이는 상태에서
    HOTEL → METAL → MASS → AXON → DESK 로 매번 다른 단어에 정확히 정지

### 로고 간격 착시 보정
- **`.optical{padding-left:.05em}` 신설**
  - 변경 파일: `index.html` (`#brand .fixed`, `.brandline`, `viewAbout()`)
  - 이유: 단어와 `BY` 사이가 `BY`–`X` 사이보다 좁아 보인다는 지적. **실측하니 같았습니다**
    (아래 반증). `Y` 가 베이스라인에서 열려 있어 뒤 간격에 여백을 빌려주는 착시라
    판단하고, 실제 공백은 그대로 둔 채 보정값만 얹었습니다
  - 실측: nbsp advance **13.64px / 13.64px** (57px 기준, Range API).
    캔버스 픽셀 스캔 "BAR BY X" — **R→B 17px / Y→X 14px** (물리적으로는 앞이 더 넓음)
  - ABOUT 의 `.brandline` 은 `.blank` 가 solid bar 라 여백을 빌려줄 것이 없어 실제로도
    좁습니다. 같은 클래스를 붙였습니다. 320px 에서도 한 줄 유지 확인

## [블로그] 글쓰기 도구

### 글자색 · 배경색
- **`{글자색:#hex|글}` / `{배경색:#hex|글}` 표기 + 스와치 판**
  - 변경 파일: `index.html` (`rich()`, `safeColor()`, `inkOn()`, `SWATCHES`,
    `colorPicker()`, `applyColor()`, `clearColor()`, `.tx-bg`, `.cpick`)
  - 이유: 클라이언트 직접 요청. `PROJECT.md` §8 **#33 을 뒤집는 결정**이라 #36 에
    기록했습니다. 연 범위는 **본문 안뿐**이고 팔레트 토큰은 그대로입니다
  - **보안**: 값은 `safeColor()` 가 3/6자리 hex 만 통과시킵니다. 통과 못 하면 표기가
    통째로 사라지고 글자만 남습니다 — `style` 속성이 색 이외의 것을 실을 수 없습니다
  - 배경색은 `inkOn()` 이 명도(0.299R+0.587G+0.114B, 임계 150)로 흑/백 글자를 정합니다.
    어두운 배경에 검은 글자가 남는 조합이 나오지 않습니다
  - 스와치 판은 **재렌더하지 않고** `hidden` 속성만 토글합니다. 재렌더하면 선택이 날아가
    "고른 글자에 색 입히기"가 불가능해집니다
  - 실측: `{글자색:#e02b2b|빨강}` → `<span style="color:#e02b2b">빨강</span>`,
    `{배경색:#1f6feb|…}` → `color:#ffffff` 자동. `{글자색:red|글}` → `글` (표기 소멸)

### 밑줄 · 코드 · 목록 · 번호목록 · 구분선
- **인라인 `__밑줄__`, `` `코드` `` / 블록 `- `, `1. `, `---`**
  - 변경 파일: `index.html` (`rich()`, `textToBlocks()`, `bodyToText()`,
    `postBodyHTML()`, `listBlock()`, `.art ul/ol/hr/code/u`)
  - 이유: 블로그에 목록이 없던 것이 가장 큰 구멍이었습니다
  - **`textToBody()` 가 종류·정렬이 같은 이웃 목록 블록을 합칩니다.** 항목 사이에 빈 줄을
    넣고 쓰는 게 자연스러운데 그대로 두면 `<ol>` 이 매번 1부터 다시 시작합니다.
    파싱은 `textToBlocks()`, 병합은 `textToBody()` 로 나눴습니다
  - `wrapSelection()` 이 `['앞','뒤']` 배열을 받게 확장했습니다 — 색 표기는 여는 쪽에
    `|` 가 들어 있어 기존 `'앞|뒤'` 문자열 형태로는 쪼개지지 않습니다
  - `prefixBlock()` 을 `blockAt()` / `replaceBlock()` 으로 분해하고 `listBlock()` 을 얹었습니다
  - 실측: 왕복 무손실 — `1. 하나\n\n1. 둘\n\n1. 셋` → `1. 하나\n2. 둘\n3. 셋`.
    **시드 글 6편 전부 왕복 무손실 유지**

### 입력창 배경
- **`color-scheme:light` + 폼 컨트롤 배경 명시**
  - 변경 파일: `index.html` (`:root`, `input,textarea,select`, `.wbody`, `.wbody.on`)
  - 이유: 글쓰기 칸이 검게 보인다는 제보. 원인은 코드가 아니라 **UA 다크 스킴**이었습니다
    (아래 반증). 흰 종이/검은 잉크를 못 박아 브라우저가 뒤집지 못하게 했습니다
  - `.wbody.on`(드래그 오버)이 검은 배경이었던 것도 점선 테두리로 바꿨습니다.
    드롭 없이 드래그가 끝나면 "칸이 검게 변한" 것처럼 보일 여지가 있었습니다
  - 실측: `#wbody` / `.fld input` 계산값 `rgb(255,255,255) / rgb(0,0,0)`

## [ABOUT] 구성원

### 프로필 사진 · 이력
- **한 줄 = 한 사람, `이름 | 직책 | 이력 | 사진주소`**
  - 변경 파일: `index.html` (`cells()`, `peopleShots()`, `setPersonPhoto()`,
    `viewAbout()`, `edSettings()`, `DEFAULT_ABOUT.people`, `.person .bio`, `.pshots`)
  - 이유: 사진·이력을 넣을 자리가 없었음. **사진을 위치 기반 별도 배열로 두지 않은 것이
    핵심 결정**입니다 — 이름을 지우거나 옮기는 순간 짝이 어긋납니다. 한 줄에 붙어 있으면
    어긋날 수가 없습니다 (`PROJECT.md` §8 #38)
  - `pairs()` 는 첫 `|` 에서만 자르므로 `cells()` 를 새로 만들었습니다.
    **전역 이름을 `cols` 로 두면 `svgArt()`/`viewProjects()` 의 지역 변수와 겹쳐** `cells` 로 명명
  - 사진 타일은 이름 칸을 **떠날 때**(`onchange`) 다시 그립니다. 타이핑 중에 그리면 포커스 유실
  - 이력의 ` / ` 는 공개 뷰에서 줄바꿈이 됩니다
  - 실측: `setPersonPhoto` 왕복 6종 통과 —
    `PARK | P | bio` + `u.webp` → `PARK | P | bio | u.webp`, 삭제 시 뒤 칸 제거,
    다른 사람 줄 불변
- **`.people` 그리드 `auto-fill` → `auto-fit`**
  - 변경 파일: `index.html`
  - 이유: `gap+검은 배경` 그리드에서 빈 트랙이 검은 판때기로 드러났습니다.
    이력이 붙어 카드가 길어지면서 눈에 띄게 커졌습니다

## [프로젝트] 참여자 · 썸네일

### 참여자
- **`projects.people` 열 추가, `역할 | 이름` 한 줄에 하나**
  - 변경 파일: `index.html` (`edProject()`, `viewProjectDetail()`, `blank()`, `SEED_PROJECTS`)
  - 이유: 크레딧을 적을 자리가 없었음. 상세 페이지 스펙 표(`.specs dl`) 뒤에 이어집니다
  - `|` 가 없는 줄은 `Credit` 으로 들어갑니다

### 썸네일 유형 배지 삭제
- **`.card .badge` 마크업·CSS 제거**
  - 변경 파일: `index.html` (`cardHTML()`, `.card .badge`)
  - 이유: 클라이언트 요청. 유형은 INDEX 뷰와 프로젝트 페이지에 이미 있고,
    사진 위에서는 작품에 붙인 라벨처럼 읽힙니다

## [소식] 사진 · 링크

### 이미지와 외부 링크
- **`news.img` / `news.url` 열 추가**
  - 변경 파일: `index.html` (`viewNews()`, `edNews()`, `hostOf()`, `blank()`, `SEED_NEWS`,
    `.row-i.pic`, `.row-i .th`, `.row-i .out`)
  - 이유: 기사 링크를 걸 방법이 없었음. `url` 이 있으면 행 자체가 `<a target="_blank">` 가
    되고, `SAFE_HREF()` 를 통과 못 하면 링크가 되지 않고 그냥 행으로 남습니다
  - `hostOf()` 로 뽑은 도메인을 행 안에 적어 어디로 가는지 미리 보입니다
  - **한 건이라도 사진이 있으면 모든 행이 사진 칸을 유지합니다**(`.th.empty`, `height:0`).
    처음엔 사진 있는 행만 칸을 줬는데 제목의 왼쪽 끝이 행마다 들쭉날쭉했습니다
  - 실측: 링크 행 `tagName=A`, `target=_blank`, `chosun.com ↗` 표시, 사진 행 4건

## [CONTACT] 이메일 · 지도

### 이메일 클립보드 복사
- **`mailto:` 링크 → `[data-copy]` 버튼**
  - 변경 파일: `index.html` (`viewContact()`, `bindPage()`, `.copymail`)
  - 이유: 클라이언트 요청 — "outlook 연결되는 게 불편함". 설정한 적 없는 메일
    클라이언트가 열리는 것보다 주소를 건네받는 편이 낫습니다
  - `navigator.clipboard` → 실패 시 숨은 textarea + `execCommand('copy')` **2단 폴백**.
    첫 번째는 보안 컨텍스트와 **포커스된 문서**를 요구해 조용히 실패할 수 있습니다
  - 실측: 실제 클릭에서 성공, 라벨 `복사됨` 전환·토스트 확인.
    (JS `b.click()` 은 사용자 제스처가 없어 1단계 실패 → 폴백이 필요한 이유를 그대로 보여줌)

### 지도
- **구글 키리스 임베드 + 다크 그레이 + 영문 라벨**
  - 변경 파일: `index.html` (`mapHTML()`, `mapQuery()`, `DEFAULT_CONTACT.mapq`,
    `edSettings()`, `.map iframe`, `.maplinks`)
  - 이유: 클라이언트 요청 — "네이버지도 아니면 구글지도도 괜찮음, 쉬운 방법으로".
    네이버는 API 키 + 스크립트가 필요합니다. 구글 `?q=…&output=embed` 는 키도 스크립트도
    없이 iframe 하나 — **다만 외부 로드가 하나 늘었습니다** (`PROJECT.md` §8 #40)
  - 다크 그레이는 **CSS 필터**입니다. 키리스 임베드는 style 파라미터를 받지 않으므로
    `grayscale(1) invert(1) contrast(.86) brightness(1.02)` — 밝은 지도를 뒤집으면
    어두운 지도에 밝은 라벨이 됩니다
  - **호버에서 밝은 지도로 되돌리지 않습니다.** 어두운 화면에서 흰색이 번쩍입니다
  - 라벨은 `hl=en`. 한국 장소는 구글이 영문 아래 국문을 함께 붙이는데, 이건 구글의
    데이터 표시 방식이라 파라미터로 끌 수 없습니다
  - 약도 이미지를 올리면 지도 대신 그 그림이 나갑니다 — **코드 변경 없이 되돌릴 수 있는 길**
  - `mapq`(지도에서 찾을 주소) 칸 신설. 비면 `address` 를 씁니다

## [디자인] 커서 · 파비콘 · 이름

### 흑백 반전 커서
- **화살표 18×24 / 손 26×24 / I빔 16×24, SVG 데이터 URI**
  - 변경 파일: `index.html` (`/* ---------- CURSOR ---------- */` 블록)
  - 이유: 클라이언트 요청 — 검은 몸통에 흰 선. OS 커서는 색 지정이 불가능해
    이미지 공급이 유일한 방법입니다. 흰 종이 위에서 기본 커서가 구멍처럼 보이던 문제도 함께 해결
  - **흰 복사본을 굵은 흰 선(stroke-width 4)으로 먼저 깔고 그 위에 검은 도형**을 얹습니다.
    도형마다 따로 stroke 를 주면 겹치는 부분에 흰 이음매가 생깁니다
  - `url()` 뒤 두 숫자는 핫스팟, 맨 뒤 키워드는 이미지 거부 시 폴백입니다
  - 실측: `body` 18px/`default`, 버튼 26px/`pointer`, 입력칸 16px/`text` 계산값 확인

### 파비콘
- **`BYX_favicon.svg` 를 base64 로 내장**
  - 변경 파일: `index.html` (`<link rel="icon">`), `BYX_favicon.svg` (신규, 원본 보관)
  - 이유: 클라이언트가 직접 만든 파비콘 제공. 단일 파일 원칙상 별도 요청이 생기면 안 되므로 내장
  - 주석·title·desc·공백 제거 후 인코딩: **1,997 → 1,197 bytes → base64 1,596자**
  - 실측: 120/32/16px 렌더 확인 — 네 원 안의 B·X·Y 가 16px 에서도 형태가 읽힘
  - 원본 파일을 고쳐도 사이트는 바뀌지 않습니다. 다시 인코딩해 그 줄을 교체해야 합니다

### 탭 제목 · 메뉴 이름
- **`BY X — Architecture & Design` → `BYX`, 페이지는 `BYX│NEWS`**
  - 변경 파일: `index.html` (`<title>`, `render()`)
  - 이유: 클라이언트 요청. 구분 기호를 가로선(`—`)에서 세로선(`│` U+2502)으로,
    순서도 사무소 이름이 앞으로
- **PROJECT 메뉴 → WORKS**
  - 변경 파일: `index.html` (`#nav`, `#mobnav`, `render()`)
  - 이유: 클라이언트 요청. **라벨만 바꾸고 라우트·`data-p` 는 `project` 그대로** 두었습니다.
    주소가 바뀌면 이미 나간 링크가 깨집니다

## [데이터] 스키마 내성

### 없어도 되는 칸
- **`DB.cols` 판정 + `DB.save()` 의 선택적 제거 + `miss()` 안내**
  - 변경 파일: `index.html` (`DB.cols`, `DB.refresh()`, `DB.save()`, `miss()`, `hintFor()`)
  - 이유: 이번에 열 3개(`projects.people`, `news.img`, `news.url`)가 늘었는데
    **클라이언트가 SQL 을 실행하기 전에도 나머지는 저장돼야 합니다.**
    `settings` 표를 "없어도 되는 표"로 설계한 것과 같은 태도입니다
  - `select('*')` 는 값이 null 인 열도 키로 돌려주므로 **첫 행의 키 존재 여부**로 판정합니다.
    행이 0개면 판정 근거가 없어 "있다"로 두고, 저장 실패 시 `hintFor()` 가 SQL 을 안내합니다
  - 편집 화면에서는 해당 칸 바로 아래에 검은 안내 상자가 뜹니다

## [문서] 인수인계 · 운영 매뉴얼

### PROJECT.md
- §2 파일 구조에 `BYX_favicon.svg` 추가 (원본 보관용이며 고쳐도 반영 안 됨을 명시)
- §4-1 에 `color-scheme:light` 의 이유, `auto-fit` 규칙, 본문 색 예외, 커서 2중 렌더 기록
- §5-6 에 새 블록·인라인 표기와 색 값 검증, 스와치 판이 재렌더하지 않는 이유 추가
- **§5-9 ~ §5-12 신설** — 구성원·참여자 / 소식 사진·링크 / 복사·지도 / 로고 `.optical`
- §6-2 스키마에 새 열 3개와 `DB.cols` 폴백 설명 추가
- **§8 에 #36 ~ #45 추가.** #33(색 금지)은 취소선 처리하고 뒤집힌 곳을 가리킴
- §9 에 SQL 실행 항목 추가, favicon 완료 처리, 지도 주소 항목 추가
- §10 "조심할 것"에 8줄 추가 (`cols`/`cells` 이름 충돌, 새 열 3점 세트 등)

### CLAUDE.md
- 파일 목록·줄 수 갱신, 코드 규칙 9줄 추가, "하지 말 것"에 배지·mailto 재도입 금지 추가

### 배포-가이드.md
- 3-2 SQL 에 새 열 3개 추가, 재실행 안내 상자를 **네 줄 + 표**로 확장
- **4-3 ~ 4-6 신설** — 구성원 사진·이력 / 프로젝트 참여자 / 소식 사진·링크 / 이메일·지도
- 4-2 표에 밑줄·코드·목록·번호목록·구분선 추가, **글자에 색 넣기** 절 신설
- 문제 해결 표에 3줄 추가 (안내 상자, 검은 입력창, 지도 오위치)

## 반증

원인이 코드가 아니었던 것들입니다. **같은 길로 다시 들어가지 마세요.**

- **글쓰기 칸이 검게 보이는 것 — 코드에 검은 배경이 지정되어 있어서가 아님.
  반증 완료(원인 아님, 반복 금지).**
  `.wbody` 에는 배경 지정이 없었고 `background:transparent` 였습니다.
  드래그 상태 `.wbody.on` 도 범인이 아니었습니다(dragleave 가 정상 해제).
  진짜 원인은 **OS/브라우저가 다크 모드일 때 UA 가 폼 컨트롤에 자기 어두운 캔버스를
  칠하는 것**입니다. `color-scheme:light` 와 배경 명시가 처방입니다
- **로고 간격이 실제로 좁은 것이 아님. 반증 완료(원인 아님, 반복 금지).**
  Range API 실측 **13.64px / 13.64px 동일**, 캔버스 픽셀 스캔은 오히려
  **R→B 17px / Y→X 14px 로 앞이 더 넓음**. `padding` 값이 잘못된 것도,
  `fitItems()` 의 `scaleX` 때문도 아닙니다(transform-origin 이 우측이라 우변 고정).
  `Y` 의 베이스라인 개방부가 만드는 착시이므로 **보정값으로만** 다룰 것
- **지도 좌상단 검은 상자 — 필터나 iframe 설정 문제가 아님. 반증 완료(원인 아님, 반복 금지).**
  더미 주소(`2F, 14 Seongsui-ro…`)가 지오코딩에 실패해 장소 카드가 로딩 상태로
  남은 것입니다. 실제 주소(`서울특별시 성동구 성수이로 14`)로 바꾸니 영문 장소 카드가
  어두운 톤으로 정상 표시됐습니다. **데이터 문제입니다**
- **브라우저 자동화에서 슬롯이 멈추지 않는 것 — 회전 로직 문제가 아님.
  반증 완료(원인 아님, 반복 금지).**
  `document.hidden === true` 인 백그라운드 탭에서는 Web Animations 가 진행되지 않아
  `anim.onfinish` 가 호출되지 않고 `spinning` 이 `true` 로 남습니다.
  **슬롯 관련 검증은 반드시 탭이 보이는 상태에서** 하세요

## 검증 결과

- **단위 테스트 56개 전부 통과.** 스크립트를 추출해 Node 에서 순수 함수만 돌립니다
  (`rich()`, `textToBody()`/`bodyToText()`, `cells()`, `setPersonPhoto()`, `hostOf()`,
  `mapQuery()`, `safeColor()`, `inkOn()`)
  - 시드 글 6편 **왕복 변환 무손실**
  - XSS 5종 무력화 — 스크립트 태그, 생 HTML, `javascript:` 링크,
    색 값으로 속성 탈출 시도 2종(공백 포함/미포함)
  - 목록 병합 5종, 사진 칸 배치 6종
- **브라우저(Chrome, 로컬 정적 서버 `http://localhost:8731`, 데모 모드 사본)**
  - 전 라우트 렌더 + **콘솔 에러 0** (9개 라우트 순회)
  - 데스크톱 1440 / 모바일 390·320 — 가로 넘침 없음
    (`scrollWidth` 372px, CONTACT 만 387px)
  - ADMIN 로그인 → 글 편집 → 색·목록·구분선 적용 → 미리보기 동기화 →
    구성원 사진 지정 → 저장 → 공개 페이지 반영까지 왕복
  - 글쓰기 중 **포커스·커서 위치 유지** 확인 (`document.activeElement.id === 'wbody'`)
  - 라우트 이동마다 회전, 화면에 보이는 상태에서 정확히 정지
  - 지도 다크 그레이 + 영문 라벨, 이메일 복사 동작, 커서 3종 계산값
- **미검증**: 실제 Supabase 에 로그인해 저장하는 경로는 이번에도 확인하지 못했습니다
  (계정 없음). 데모 모드 왕복까지만 확인했습니다. **새 열 3개의 실제 저장도 미검증**입니다

## 다음 작업

### ★ 다음 세션 예정
- **Supabase SQL 실행** (클라이언트) — `projects.people`, `news.img`, `news.url`.
  배포 가이드 3-2 의 재실행 상자. 실행 전까지 그 세 칸만 저장되지 않습니다
- **로그인 상태 실제 저장 확인** — 지난 세션부터 이월. 계정이 필요합니다.
  이번에 열이 늘었으므로 참여자·소식 사진·링크까지 함께 확인해야 합니다

### 그 외 이월
- 실제 프로젝트 사진·텍스트 입력
- ABOUT/CONTACT 기본값이 여전히 더미 (구성원 이력도 지어낸 값입니다)
- **CONTACT 주소가 더미라 지도가 엉뚱한 곳을 가리킵니다.** 실제 주소 입력 필요
- OG 이미지
- 도메인 연결
- 국문/영문 전환 필요 여부 결정
- 검색엔진 노출(해시 라우팅 + 클라이언트 렌더), 접근성 미검증 — 기존 한계 그대로

---

# 2026-08-18

ADMIN을 "따로 들어가서 고치는 화면"에서 "보던 페이지에서 바로 고치는 도구"로 바꾼 날입니다.
SETUP 탭처럼 최초 1회만 쓰는 화면을 걷어내고, 대신 각 공개 페이지에 로그인했을 때만
보이는 편집바를 붙였습니다. ABOUT/CONTACT가 하드코딩이라 클라이언트가 손댈 수 없던 문제는
`settings` 표를 새로 만들어 해결했고, 이 때문에 Supabase 스키마 변경이 한 번 필요했습니다
(사용자가 직접 실행, 확인 완료).

후반부는 BLOG 개편입니다. 클라이언트가 the-edit.co.kr을 참고 사이트로 제시해서 실제로 열어
구조를 뜯어본 뒤, 커버 고정 스크롤·가운데 칼럼·인라인 서식·이미지 스와이프 묶음 네 가지를
가져왔습니다. 참고 사이트는 Swiper.js를 쓰지만 CSS scroll-snap으로 대체해 의존성은
supabase-js 하나 그대로 유지했습니다.

막힌 곳은 캐러셀이었습니다. 화살표가 동작하지 않아 한참 팠는데 원인이 두 개 겹쳐 있었고,
둘 다 코드 문제가 아니었습니다 (아래 반증 항목 참고).

## [ADMIN] 관리 화면 구조

### SETUP 탭 제거
- **탭 구성을 PROJECT / NEWS / BLOG / ABOUT / CONTACT 로 재편**
  - 변경 파일: `index.html` (`TABS`, `viewAdmin()`, `adminHelp()` 삭제)
  - 이유: 최초 1회만 보는 설정 안내가 상시 탭을 차지하고 있었음. SQL 전문은
    `배포-가이드.md` 3-2에 이미 있으므로 중복
  - 연결 상태는 문제가 있을 때만 ADMIN 상단 경고 상자로 표시하도록 변경.
    `hintFor()` 는 살려서 한국어 해결책을 계속 붙임

### 페이지 내 편집 버튼
- **`editBar(page, arg)` 신설 — 로그인 시에만 렌더**
  - 변경 파일: `index.html` (`render()`, `editBar()`, `gotoAdmin()`, `leaveEditor()`)
  - 이유: 보고 있는 항목을 고치려고 ADMIN 목록에서 다시 찾는 왕복이 불필요
  - `gotoAdmin('one:posts:<id>')` 한 문자열로 탭·드래프트를 세팅하고, `AD.back` 에
    원래 해시를 담아 저장·취소 후 그 페이지로 복귀
  - 실측: 비로그인 상태 전 라우트에서 편집바 미노출 확인

### ABOUT / CONTACT 편집 가능화
- **`settings` 표 도입 (`k text pk, v jsonb`)**
  - 변경 파일: `index.html` (`DEFAULT_ABOUT`, `DEFAULT_CONTACT`, `DB.loadSettings()`,
    `DB.saveSettings()`, `edSettings()`, `viewAbout()`, `viewContact()`)
  - 이유: 두 페이지가 하드코딩이라 클라이언트가 손댈 수 없었음. `PROJECT.md` §9의
    미결정 항목을 확정
  - **없어도 되는 표로 설계**: 조회 실패는 `DB.settingsError` 에만 기록하고 기본값으로
    넘어감. 표가 없다고 사이트 전체가 데모 모드로 떨어지면 안 되기 때문
  - 목록형 항목은 `키 | 값` 한 줄에 하나. PEOPLE 추가·삭제도 이 방식으로 동작 확인

## [ADMIN] 입력 편의

### 정렬값 입력란 제거
- **`sort` 를 저장 시 날짜에서 계산**
  - 변경 파일: `index.html` (`dnum()`, `ymNum()`, 저장 핸들러, `blank()`)
  - 이유: 날짜가 이미 순서를 결정하는데 같은 뜻을 두 곳에 적게 하면 어긋남
  - 프로젝트는 `year*100+month`, 소식·글은 `YYYYMMDD`. 열은 Supabase 정렬을 그대로
    쓰기 위해 남겨둠

### 프로젝트 유형 자유 입력
- **`<select>` → `<input list>` + `<datalist>`**
  - 변경 파일: `index.html` (`SUG()`, `typeOptions()`)
  - 이유: 고정 목록이라 새 유형을 넣을 수 없었음. 기존 값은 자동완성으로 돕고
    없는 값은 그대로 새 유형이 됨

### 프로젝트 코드·날짜
- **코드 `BYX-` 접두 제거**
  - 변경 파일: `index.html` (`SEED_PROJECTS`, `blank()`)
  - 이유: 사무소 사이트에서 사무소 약자를 매 항목 반복할 이유가 없음
- **연도 → 연월 (`month` 열 추가)**
  - 변경 파일: `index.html` (`ymText()`, `ymInput()`, `filtered()`, 카드·INDEX·상세)
  - 이유: 같은 해 프로젝트의 선후가 보이지 않았음
  - 실측: INDEX 오름차순 `2023.01 → 2023.02 → 2023.03 → 2024.01`, 내림차순 역순 확인

## [ADMIN] 이미지 파이프라인

### 업로드 자동 축소
- **`shrink(file)` 신설, `DB.upload()` 가 항상 선행 호출**
  - 변경 파일: `index.html`
  - 이유: 카메라 원본을 그대로 올리면 Supabase 무료 플랜(1GB/5GB)이 금방 참
  - 기준: 긴 변 2560px(QHD 전체 폭이 최대 슬롯), WebP q0.82
  - 실측: **6000×4000 / 2.9MB → 2560×1707 / 326KB (10.9%)**
  - GIF·SVG는 통과(리샘플 시 깨짐). 결과가 원본보다 크면 원본 유지.
    `file.shrunk` 플래그로 멱등 — 호출부에서 미리 줄여도 두 번 인코딩되지 않음
  - WebP 미지원 시 JPEG 폴백이며 알파가 없으므로 흰 배경을 먼저 칠함

## [BLOG] 글쓰기 방식

### 블록 UI → 한 칸에 쭉 쓰기
- **`bodyToText()` / `textToBody()` 양방향 변환 도입**
  - 변경 파일: `index.html` (`edPost()`, 블록 추가·이동 UI 및 관련 바인딩 삭제)
  - 이유: 문단마다 블록을 만들고 ↑↓로 옮기는 방식이 글쓰기 리듬을 끊음
  - **저장 형태는 블록 배열 그대로 유지** — 공개 뷰가 그걸 읽으므로 기존 글이 안전
  - 실측: 시드 글 6편 전부 왕복 변환 무손실

### 인라인 서식
- **`rich()` 신설 — `esc()` 뒤에 표기만 태그로 치환**
  - 변경 파일: `index.html`
  - 이유: 텍스트 자유도 요청. contenteditable 리치 에디터는 붙여넣기 오염 정리
    코드가 본체보다 커지고 "빌드 없는 단일 파일" 제약과 맞지 않음
  - 표기: `**굵게**` `*기울임*` `~~취소선~~` `==강조==` `((흐리게))` `@@제목체@@` `[글](주소)`
  - **색 선택 기능은 넣지 않음** — 팔레트가 네 토큰뿐이라 임의 색을 열면 시스템이 무너짐.
    `==강조==`(흑백 반전)와 `((흐리게))` 로 대체. 사용자 확인 대기 중인 판단
  - 실측(보안): `<script>` 이스케이프됨, `<b>` 이스케이프됨, `javascript:` 링크는 `#` 로
    무력화됨. **`esc()` → 치환 순서를 뒤집으면 XSS 구멍**이라 문서에 못 박음
- **블록 정렬 `[가운데]` / `[오른쪽]` 접두**
  - 변경 파일: `index.html` (`textToBody()` 가 `b.a` 로 파싱, `alignClass()`)
  - 이유: 정렬은 인라인이 아니라 블록 단위 속성

### 편집 툴바 · 미리보기
- **`wrapSelection()` / `prefixBlock()` / `syncBody()`**
  - 변경 파일: `index.html`
  - 이유: 표기를 외우지 않아도 되게. 셋 다 재렌더하지 않음(커서·포커스 유실 방지)
  - `prefixBlock()` 은 커서가 있는 블록에만 토큰을 붙이고 기존 토큰을 배타적으로 교체
  - 미리보기는 공개 뷰와 **같은 `postBodyHTML()`** 을 씀
  - 실측: 타이핑 중 포커스·커서 위치 유지 확인, 3문단 중 가운데 문단에만 정렬 적용 확인

## [BLOG] 읽기 화면 — the-edit.co.kr 참고

### 참고 사이트 실측
- 커버는 `.single-header` 가 `position:fixed; height:100vh`, 본문 시트가 그 위로 올라옴
- 본문 칼럼 **800px 고정폭 중앙 정렬** (2560px 화면에서도 800px), 본문 16px/28px
- 이미지 묶음은 **Swiper.js**(WordPress uagb-slider 블록), 화살표 + 페이지네이션

### 포스트 커버
- **`.post-cover` sticky + `.post-sheet` 오버레이**
  - 변경 파일: `index.html`
  - 이유: 글 진입에 호흡을 주기 위해. 스크립트 없이 CSS만으로 구현
  - 커버는 분류·날짜 / 큰 밑줄 제목 / 한 줄 요약 / SCROLL 표시로 **자동 생성**
  - `100svh` 앞에 `100vh` 폴백 줄을 둠

### 본문 가운데 정렬
- **`.art` 를 `max-width:78ch; margin-inline:auto` 로 변경**
  - 변경 파일: `index.html`
  - 이유: 긴 글은 좌측 정렬 전면폭보다 가운데 좁은 칼럼이 읽힘

### 이미지 스와이프 묶음
- **연속된 이미지 줄 = 한 묶음 (`{t:'gal', items:[…]}`)**
  - 변경 파일: `index.html` (`galleryHTML()`, `bindGalleries()`, `insertImagesAt()`)
  - 이유: 묶음 전용 문법을 만들지 않기 위해. 사진을 여러 장 한 번에 넣으면 그냥 묶음이 됨
  - CSS `scroll-snap-type:x mandatory` 한 줄이 터치 스와이프를 전부 처리.
    **Swiper.js를 쓰지 않아 외부 의존성은 supabase-js 하나 그대로**
  - 실측: 화살표 1→2→3→4 이동, 양 끝에서 버튼 비활성, 네이티브 스크롤 시
    카운터·점 동기화 확인

## [디자인] 세부 조정

### 로고 자간
- **`padding-left:.15em` → 실제 공백 문자**
  - 변경 파일: `index.html` (`#brand .fixed`, 마크업 `&nbsp;BY&nbsp;X`)
  - 이유: 흉내 낸 값이 `BY X` 의 공백과 미세하게 달랐음. 실제 공백을 쓰면 정의상 같아짐
  - 실측: Range API로 측정 — 헤더 **13.66px / 13.66px**, ABOUT **38.55px / 38.55px** 일치

### ABOUT 머리글
- **`____ BY X` 를 한 줄로**
  - 변경 파일: `index.html` (`.brandline`, `viewAbout()`)
  - 이유: 두 줄로 접히면 로고가 아니라 문단처럼 읽힘
  - 실측: 콘텐츠 폭이 3.95em이라 320px~2560px 전 구간에서 한 줄 유지(계산 검증)

### CONTACT 정리
- 대문짝만한 이메일(`.bigmail`) 삭제, 메일 3분할 → 대표 메일 1개,
  VISIT 하단 INSTAGRAM/BEHANCE/LINKEDIN 삭제
  - 변경 파일: `index.html`
  - 이유: 클라이언트 요청. 소셜은 푸터에 인스타 아이콘이 이미 있음

### 한글 굵기 보정 누락 수정
- **`.post-cover h1`, `.art .tx-disp` 를 `font-variation-settings:'wght' 680` 목록에 추가**
  - 변경 파일: `index.html`
  - 이유: Anton에 한글 글리프가 없어 `@@제목체@@` 가 한글일 때 SUIT로 폴백되며 얇아짐.
    프로젝트 규칙(`CLAUDE.md`)이 요구하는 조치

## [버그] 기존 결함 수정

### BLOG 빈 목록 백지
- **글이 0건일 때 안내문 추가**
  - 변경 파일: `index.html` (`viewBlog()`)
  - 이유: PROJECT·NEWS에는 안내문이 있는데 BLOG만 완전 백지였음.
    실제 Supabase에 글이 없어 발견

### ABOUT 잉여 문자
- `viewAbout()` 템플릿 끝에 있던 `>` 한 글자 제거 (화면에 그대로 렌더되고 있었음)

## [데이터] Supabase 스키마

- **`projects.month` 열 추가 + `settings` 표 생성** — 사용자가 직접 SQL 실행 (2026-08-18)
- 실측 확인:
  - `projects.month` 존재 — `select id,year,month` 성공
  - `settings` 조회 성공, **0행** (아직 저장 전이라 정상)
  - `DB.settingsError` = `null`, ADMIN 경고 상자 0개
  - 익명 upsert 차단 — **에러코드 42501 (insufficient_privilege)**. RLS 정책 정상

## 검증 결과

브라우저(Chrome, 로컬 정적 서버 `http://localhost:8731`)로 확인했습니다.
`file://` 은 확장이 차단해서 정적 서버를 띄워야 했습니다.

- 전 라우트 렌더 + **콘솔 에러 0** — 데모 모드와 실제 Supabase 연결 양쪽
- 시드 글 6편 왕복 변환 무손실
- 인라인 서식 전체 렌더 + XSS 3종(스크립트 태그, 생 HTML, `javascript:`) 무력화
- 캐러셀 이동·정지·스와이프 동기화
- 편집 툴바 전 기능(선택 감싸기, 빈 커서 위치, 블록 토큰 배타 교체, 블록 단위 정렬)
- 글 작성 → 저장 → 공개 페이지 반영 (end-to-end)
- 로그인 시 편집바 노출 / 비로그인 시 미노출
- ABOUT PEOPLE 추가·삭제 → 저장 → 원래 페이지 복귀
- 이미지 축소 2.9MB → 326KB

**미검증**
- **로그인 상태의 실제 쓰기** — 계정이 없어 익명 읽기까지만 확인. ABOUT 저장은
  사용자가 한 번 눌러봐야 최종 확인됩니다
- **모바일 실제 폭(390px)** — `resize_window` 가 이 환경에서 `innerWidth` 에 반영되지
  않아 실측 불가. ABOUT 머리글만 clamp 계산으로 검증했고 나머지는 계산·코드 검토 수준
- 부드러운 스크롤 애니메이션의 실제 모습 (아래 반증 항목 참고)

## 반증된 가설 (반복 금지)

- **"캐러셀 화살표가 안 먹는다 = scroll-snap 이 되돌린다"** — **반증 완료(원인 아님)**.
  스냅을 완전히 끈 격리 테스트에서도 동일하게 실패했습니다.
- **"scroll-behavior:smooth / scrollIntoView 로 바꾸면 된다"** — **반증 완료(원인 아님)**.
  둘 다 같은 경로라 동일하게 실패.
- **실제 원인 두 가지**
  1. **자동화 탭이 `document.hidden === true`** — `requestAnimationFrame` 이 아예 돌지
     않고 UA 스무스 스크롤이 취소됨. `setTimeout` 도 1초로 클램프되어 폴백까지 늦어짐.
     **코드 문제가 아니었음.** 애니메이션이 안 되는 것처럼 보이면 `document.hidden` 부터
     확인할 것
  2. **브라우저가 이전 `index.html` 을 캐시** — 고친 코드가 반영되지 않아 "여전히 안 된다"로
     보였음. 주소에 `?v=2` 를 붙이자 즉시 정상 동작. **로컬 확인 시 캐시 무효화 필수**
- 다만 이 과정에서 실제 문제를 하나 발견해 고쳤습니다: **떠 있는 스무스 스크롤이 뒤이은
  `scrollLeft` 대입까지 삼켜** 폴백이 무력화됩니다. 그래서 `step()` 은 rAF로 직접
  트윈하고, 애니메이션이 불가능하면 즉시 대입하도록 바꿨습니다.

## 다음 작업

### ★ 다음 세션 예정
- **로그인해서 ABOUT 저장 한 번 확인** (위 미검증 항목). 실패 시 RLS 정책 재확인
- 본문 서식에 **자유 색상**을 열지 여부 결정 — 현재는 팔레트 규칙(§4-1)을 지켜
  `==강조==` / `((흐리게))` 로만 제공. 규칙을 바꾸는 결정이라 사용자 판단 필요
- 마지막 요청 메시지가 `-` 로 끝나 있어 **누락된 항목이 있는지 확인**

### 그 외 이월
- ABOUT/CONTACT 기본값이 아직 지어낸 더미 — 실제 내용으로 교체 (이제 ADMIN에서 가능)
- 실제 프로젝트 사진·텍스트 입력
- OG 이미지, 실제 favicon, 도메인 연결
- 국문/영문 전환 필요 여부 결정
- 모바일 390px 실기 확인
- SEO: 해시 라우팅이라 개별 글이 색인되지 않음 (중요해지면 검토)
