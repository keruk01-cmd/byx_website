# BYX 웹사이트 배포 가이드

GitHub → Vercel → Supabase 순서로 진행합니다. 터미널(검은 명령창)은 한 번도 쓰지 않습니다.
전부 브라우저에서 클릭과 붙여넣기만 합니다. 총 소요 시간 약 40분.

> **왜 이 순서인가**
> 1단계와 2단계만 끝내도 사이트가 인터넷에 뜹니다(데모 모드). 눈으로 확인하고 나서
> 3단계 Supabase를 붙이는 편이 중간에 무엇이 잘못됐는지 찾기 쉽습니다.

---

## 준비물

| 항목 | 비용 | 비고 |
|---|---|---|
| GitHub 계정 | 무료 | 소스 보관 |
| Vercel 계정 | 무료 (Hobby) | 웹 호스팅. GitHub 계정으로 가입 |
| Supabase 계정 | 무료 | 데이터베이스 + 이미지 저장 + 로그인 |
| `index.html` | — | 이 대화에서 받은 파일 |
| `vercel.json` | — | 같이 받은 6줄짜리 설정 파일 |

세 서비스 모두 **GitHub 계정 하나로 로그인**할 수 있으니, GitHub부터 만드세요.

---

# 1단계 — GitHub에 올리기 (약 10분)

## 1-1. 계정 만들기
1. <https://github.com> 접속 → **Sign up**
2. 이메일·비밀번호·아이디 입력. 아이디는 나중에 주소에 쓰이니 짧게 (`byx-studio` 등)
3. 메일로 온 인증코드 입력

## 1-2. 저장소(Repository) 만들기
1. 오른쪽 위 **＋** → **New repository**
2. **Repository name** : `byx-website`
3. **Public** 선택
   - Private로 해도 Vercel 배포는 되지만, 무료 플랜에서는 Public이 속 편합니다.
   - 소스가 공개돼도 문제 없습니다. 비밀번호는 파일에 들어가지 않습니다.
4. 아래 **Add a README file** 은 체크하지 마세요 (빈 저장소가 편합니다)
5. **Create repository**

## 1-3. 파일 올리기
빈 저장소 화면에 *"uploading an existing file"* 링크가 보입니다.

1. **uploading an existing file** 클릭
2. `index.html` 과 `vercel.json` **두 파일을 함께** 창 안으로 끌어다 놓기
3. 아래 **Commit changes** 버튼 클릭

끝입니다. 저장소에 파일 두 개가 들어 있으면 성공.

> `vercel.json` 은 `byx.kr/admin` 주소로 바로 들어갈 수 있게 해주는 6줄짜리 설정입니다.
> 이게 없으면 `/admin` 이 404가 납니다.

> **앞으로 파일을 고치는 법**
> 저장소에서 `index.html` 클릭 → 오른쪽 위 **연필 아이콘** → 수정 → **Commit changes**.
> 커밋할 때마다 Vercel이 자동으로 사이트를 다시 배포합니다.

---

# 2단계 — Vercel로 배포하기 (약 5분)

## 2-1. 가입
1. <https://vercel.com> → **Sign Up** → **Continue with GitHub**
2. 권한 요청이 뜨면 **Authorize**
3. 개인 용도이므로 **Hobby** 선택, 이름 입력

## 2-2. 프로젝트 가져오기
1. 대시보드에서 **Add New… → Project**
2. 목록에서 `byx-website` 옆 **Import**
   - 목록에 없으면 **Adjust GitHub App Permissions** → 저장소 접근 허용
3. 설정 화면이 나옵니다. **아무것도 건드리지 마세요.**
   - Framework Preset: `Other` (자동)
   - Build Command / Output Directory: 비워둠
   - HTML 파일 하나뿐이라 빌드할 게 없습니다
4. **Deploy** 클릭

30초쯤 뒤 축하 화면과 함께 주소가 나옵니다:
`https://byx-website-xxxx.vercel.app`

**여기서 한 번 확인하세요.** 사이트가 뜨고, 로고를 누르면 슬롯머신이 돌아야 정상입니다.
주소 끝에 **`/admin`** 을 붙여서 (`https://byx-website-xxxx.vercel.app/admin`) 들어가면
데모 모드 로그인 화면이 나와야 합니다.

## 2-3. (선택) 도메인 연결
`byx.kr` 같은 주소를 이미 갖고 있다면:

1. Vercel 프로젝트 → **Settings → Domains** → 도메인 입력 → **Add**
2. Vercel이 알려주는 값을 도메인 구입처(가비아·후이즈 등) DNS 설정에 입력
   - 최상위 도메인(`byx.kr`) → **A 레코드**, 값은 Vercel이 표시하는 IP
   - `www.byx.kr` → **CNAME**, 값은 `cname.vercel-dns.com`
3. 반영에 10분~수 시간. HTTPS 인증서는 Vercel이 알아서 발급합니다.

---

# 3단계 — Supabase 연결하기 (약 20분)

여기까지는 데모 모드입니다. 이제 편집한 내용이 진짜로 저장되게 만듭니다.

## 3-1. 프로젝트 만들기
1. <https://supabase.com> → **Start your project** → GitHub으로 로그인
2. **New project**
   - Name: `byx`
   - **Database Password: 만들어서 어딘가에 적어두세요.** 다시 볼 수 없습니다.
   - Region: **Northeast Asia (Seoul)**
3. **Create new project** → 준비에 1~2분

## 3-2. 표(테이블) 만들기
1. 왼쪽 메뉴 **SQL Editor** → **New query**
2. 아래 SQL을 **통째로** 붙여넣고 오른쪽 아래 **Run**

```sql
create extension if not exists "pgcrypto";

create table if not exists projects (
  id uuid primary key default gen_random_uuid(),
  code text, name text, client text, type text, loc text,
  year int, month int, status text, area text, role text,
  img text, shots jsonb default '[]'::jsonb, descr text,
  sort int default 0, created_at timestamptz default now()
);

create table if not exists news (
  id uuid primary key default gen_random_uuid(),
  d text, t text, k text, x text,
  sort int default 0, created_at timestamptz default now()
);

create table if not exists posts (
  id uuid primary key default gen_random_uuid(),
  slug text unique, d text, k text, t text, x text,
  body jsonb default '[]'::jsonb,
  sort int default 0, created_at timestamptz default now()
);

-- ABOUT / CONTACT 페이지 글을 사이트에서 고치기 위한 표
create table if not exists settings (
  k text primary key,
  v jsonb default '{}'::jsonb,
  updated_at timestamptz default now()
);

-- 보안 정책: 누구나 읽기, 로그인한 사람만 쓰기
alter table projects enable row level security;
alter table news     enable row level security;
alter table posts    enable row level security;
alter table settings enable row level security;

create policy "read all" on projects for select using (true);
create policy "write in" on projects for all using (auth.role() = 'authenticated') with check (auth.role() = 'authenticated');
create policy "read all" on news     for select using (true);
create policy "write in" on news     for all using (auth.role() = 'authenticated') with check (auth.role() = 'authenticated');
create policy "read all" on posts    for select using (true);
create policy "write in" on posts    for all using (auth.role() = 'authenticated') with check (auth.role() = 'authenticated');
create policy "read all" on settings for select using (true);
create policy "write in" on settings for all using (auth.role() = 'authenticated') with check (auth.role() = 'authenticated');
```

`Success. No rows returned` 이 나오면 성공입니다.

> ### ⚠️ 예전에 이미 이 단계를 한 적이 있다면
> 위 SQL을 그대로 다시 실행해도 안전합니다(`if not exists` 라서 이미 있는 것은
> 건드리지 않습니다). 다만 **아래 두 줄을 추가로 한 번 더 실행**해야 합니다.
> 프로젝트에 "월"이 생겼고, ABOUT/CONTACT 편집용 표가 새로 필요해졌기 때문입니다.
>
> ```sql
> alter table projects add column if not exists month int;
> ```
>
> `create policy` 부분에서 `already exists` 오류가 나면 그 줄은 무시하셔도 됩니다.
> 이미 만들어져 있다는 뜻입니다.
>
> 이걸 안 해도 사이트는 그대로 돌아갑니다. 다만 프로젝트의 **월**이 저장되지 않고,
> ABOUT/CONTACT를 편집해도 **저장되지 않습니다**.

## 3-3. 이미지 저장소 만들기
1. 왼쪽 메뉴 **Storage** → **New bucket**
2. Name: **`media`** (반드시 소문자 그대로)
3. **Public bucket** 을 **켭니다**
4. **Save**

## 3-4. ⚠️ 아무나 가입 못 하게 막기 — 이 단계를 건너뛰지 마세요
위 정책은 "로그인한 사람은 글을 고칠 수 있다"는 뜻입니다.
가입이 열려 있으면 아무나 가입해서 편집할 수 있습니다.

1. 왼쪽 메뉴 **Authentication** → **Sign In / Providers** → **Email**
2. **Allow new users to sign up** 을 **끕니다**
3. **Save**

## 3-5. 관리자 계정 만들기
1. **Authentication → Users** → **Add user → Create new user**
2. 본인 이메일과 비밀번호 입력
3. **Auto Confirm User** 를 **켭니다** (안 켜면 메일 인증 전까지 로그인 불가)
4. **Create user**

이 이메일·비밀번호가 사이트 ADMIN 로그인 정보가 됩니다.

## 3-6. 열쇠 두 개 복사하기
1. 왼쪽 아래 **Project Settings** (톱니바퀴) → **API Keys**
2. 두 값을 복사해 메모장에 붙여둡니다
   - **Project URL** — `https://abcdefgh.supabase.co`
   - **Publishable key** — `sb_publishable_...` 로 시작하는 긴 문자열
     - 이 항목이 안 보이면 **Legacy API Keys** 탭의 **anon public** 키를 쓰면 됩니다. 둘 다 동작합니다.

> `service_role` / `secret` 이라고 적힌 키는 **절대 쓰지 마세요.** 그건 관리자 전용 키라
> 공개되면 데이터베이스 전체가 열립니다. Publishable(anon) 키는 공개되어도 안전합니다.

## 3-7. 사이트에 열쇠 넣기

> **이 단계는 이미 되어 있습니다.** 받으신 `index.html` 에 아래 값이 들어가 있습니다.
> 나중에 값이 바뀔 때만 이 절차를 쓰세요.
> ```
> url: https://uvkcpzmjcpcdaeozvysv.supabase.co
> key: sb_publishable_nD-EQ8iq0PhcOwk_AB8fXw_v0kdHBsP
> ```

1. GitHub 저장소 `byx-website` → **`index.html`** 클릭
2. 오른쪽 위 **연필 아이콘(Edit this file)**
3. 파일 맨 위, **17번째 줄 근처**에 이런 부분이 있습니다:

```js
window.BYX_CONFIG = {
  url: '',        // 예: 'https://abcdefgh.supabase.co'
  key: '',        // 예: 'eyJhbGciOi...'   (anon public key)
  bucket: 'media' // Storage 버킷 이름
};
```

4. 따옴표 **안에** 값을 붙여넣습니다. 따옴표는 지우지 마세요.

```js
window.BYX_CONFIG = {
  url: 'https://abcdefgh.supabase.co',
  key: 'sb_publishable_xxxxxxxxxxxxxxxx',
  bucket: 'media'
};
```

5. 오른쪽 위 **Commit changes…** → **Commit changes**

Vercel이 자동으로 감지해서 30초 안에 다시 배포합니다.

---

# 4단계 — 확인하고 내용 채우기

1. 브라우저 주소창에 **`(내 주소)/admin`** 입력
   - 예: `https://byx-website-xxxx.vercel.app/admin` 또는 `https://byx.kr/admin`
   - 사이트 어디에도 ADMIN 링크는 없습니다. 주소를 아는 사람만 들어옵니다
2. 3-5에서 만든 이메일·비밀번호로 로그인
3. 로그인 후 화면 위쪽에 **검은 경고 상자가 없으면 성공**입니다
   - `데모 모드` 라는 검은 상자가 뜨면 → 3-7의 값을 다시 확인 (따옴표 빠짐, 오타, 공백)
   - `settings 표가 없습니다` 라고 뜨면 → 3-2의 SQL을 다시 실행 (ABOUT/CONTACT 편집에만 필요)
4. **PROJECT** 탭 → **+ New Project** 로 첫 프로젝트를 넣어봅니다
   - 이미지는 칸에 끌어다 놓으면 Supabase에 올라갑니다.
     **큰 사진은 알아서 줄여서 올라갑니다** — 원본이 8MB여도 500KB 정도가 됩니다
   - **목록 순서는 날짜가 정합니다.** 따로 맞출 값은 없습니다.
     프로젝트는 연월이 최신인 것이, 소식·글은 날짜가 최신인 것이 위로 올라갑니다
5. 저장 후 다른 브라우저(또는 휴대폰)로 사이트를 열어 그 프로젝트가 보이면 완전히 성공입니다

## 4-1. 로그인한 채로 사이트를 돌아다니면

ADMIN에 로그인한 상태로 PROJECT·NEWS·BLOG·ABOUT·CONTACT 페이지에 가면
화면 위에 **ADMIN 줄**이 하나 생기고 `이 프로젝트 편집` `+ 새 글` 같은 버튼이 보입니다.
그 버튼을 누르면 편집 화면이 바로 열리고, 저장하면 보고 있던 페이지로 돌아옵니다.
**로그아웃한 방문자에게는 이 줄이 보이지 않습니다.**

ABOUT의 소개·수상 이력·구성원, CONTACT의 주소·연락처도 이 방법으로 고칩니다.
`이름 | 직책` 처럼 세로줄(|)로 왼쪽·오른쪽을 나눠 한 줄에 하나씩 적으면 됩니다.

## 4-2. 블로그 글 쓰는 법

**BLOG → + New Post**. 본문은 큰 칸 하나에 그냥 쭉 쓰시면 됩니다.
**문단을 나눌 때는 엔터를 두 번** 칩니다(빈 줄 하나). 이것만 기억하시면 됩니다.

나머지는 전부 글 칸 위의 **버튼**이 대신 넣어줍니다. 꾸미고 싶은 글자를 마우스로
드래그해서 선택한 다음 버튼을 누르면 됩니다. 버튼이 넣는 기호는 이렇습니다.

| 버튼 | 글 칸에 들어가는 모양 | 화면에 보이는 모습 |
|---|---|---|
| **B** | `**성수동**` | **성수동** (굵게) |
| *I* | `*성수동*` | *성수동* (기울임) |
| ~~S~~ | `~~성수동~~` | ~~성수동~~ (취소선) |
| 강조 | `==성수동==` | 검은 배경에 흰 글자 |
| 흐리게 | `((성수동))` | 옅은 회색 글자 |
| 제목체 | `@@성수동@@` | 제목에 쓰는 굵은 서체 |
| 링크 | `[성수동](https://…)` | 눌리는 링크 |
| 소제목 | 줄 앞에 `##` | 중간 제목 |
| 인용 | 줄 앞에 `>` | 왼쪽에 세로선이 붙은 인용문 |
| 가운데 / 오른쪽 | 문단 앞에 `[가운데]` `[오른쪽]` | 그 문단만 정렬이 바뀜 |

기호가 그대로 보여서 어색하면 **`미리보기`** 버튼을 누르세요.
오른쪽에 실제 화면과 똑같은 모습이 뜨고, 글을 고치는 대로 바로 따라 바뀝니다.

### 사진 넣기

`＋ 이미지` 버튼을 누르거나, 사진을 글 칸에 **끌어다 놓으면** 커서 자리에 들어갑니다.
`![여기에 캡션](주소)` 에서 **대괄호 안에 설명**을 쓰면 사진 아래 캡션이 됩니다.
**괄호 안 주소는 건드리지 마세요.**

### 사진 여러 장을 좌우로 넘기게 묶기

`＋ 이미지` 에서 **사진을 여러 장 한꺼번에 고르면** 자동으로 묶음이 됩니다.
독자는 손가락으로 좌우로 밀거나(휴대폰), 양옆 화살표를 눌러(PC) 넘겨 봅니다.

글 칸에서는 이런 모양입니다. **사진 줄 사이에 빈 줄이 없으면 한 묶음**입니다.

```
![첫 장](주소1)
![둘째 장](주소2)
![셋째 장](주소3)
```

반대로 **사진 사이에 빈 줄을 넣으면** 묶이지 않고 각각 따로 큰 사진으로 놓입니다.
묶음을 풀고 싶으면 사진 줄 사이에서 엔터를 한 번 더 치시면 됩니다.

> **글의 첫 화면(커버)** 은 따로 만들 필요가 없습니다. 제목·날짜·분류·한 줄 요약으로
> 자동으로 만들어지고, 스크롤을 내리면 본문이 그 위로 덮으며 올라옵니다.
> 그러니 **Summary(한 줄 요약)를 꼭 채워주세요.** 커버에 그대로 나옵니다.

> 처음 연결하면 목록이 **비어 있는 게 정상**입니다. 데모 데이터 18개는 파일 안의 샘플이라
> 데이터베이스로 옮겨가지 않습니다. ADMIN에서 실제 프로젝트를 하나씩 넣으세요.

---

# 문제가 생기면

| 증상 | 원인과 해결 |
|---|---|
| ADMIN에 `데모 모드` | url/key 가 비어 있음. 3-7 확인 |
| ADMIN에 `연결 실패` | 화면에 **오류 메시지와 해결 방법이 함께 표시**됩니다. 그대로 따르세요 |
| ADMIN에 `settings 표가 없습니다` | 3-2의 SQL을 다시 실행. ABOUT/CONTACT 저장에만 필요합니다 |
| 프로젝트의 **월**이 저장 안 됨 | 3-2의 `alter table projects add column ... month int;` 를 실행 |
| 로그인이 안 됨 | 3-5에서 **Auto Confirm User** 를 안 켰을 가능성. Users 목록에서 해당 계정 삭제 후 다시 만들기 |
| 저장은 되는데 이미지가 안 보임 | 버킷 이름이 `media` 가 아니거나 **Public** 이 꺼져 있음 |
| `new row violates row-level security` | 로그아웃 상태. 다시 로그인. 그래도 안 되면 3-2 SQL의 정책 부분만 다시 실행 |
| Vercel 화면이 예전 그대로 | 브라우저 강력 새로고침 (`Ctrl+Shift+R` / Mac `Cmd+Shift+R`) |
| 사이트가 아예 안 뜸 | Vercel → Deployments 에서 최신 배포가 **Ready** 인지 확인 |
| `/admin` 이 404 | `vercel.json` 을 안 올렸거나 이름이 틀림. 저장소에 있는지 확인 후 재배포 |
| 파일을 그냥 더블클릭해서 열었을 때 `/admin` 이 안 됨 | 정상입니다. 이 경우엔 주소 끝에 `#/admin` 을 붙이세요 |

---

# 앞으로의 운영

- **글·프로젝트 추가/수정** → 사이트의 ADMIN에서. GitHub을 건드릴 필요 없음
- **디자인·구조 변경** → GitHub에서 `index.html` 수정 → 커밋 → 자동 배포
- **되돌리기** → GitHub 저장소 **History** 에서 이전 버전 확인 가능. 모든 수정이 기록됩니다
- **비용** → 이 규모(방문자 수천 명/월, 이미지 수백 장)면 세 서비스 모두 무료 구간 안입니다

## 백업
Supabase 무료 플랜은 자동 백업이 제한적입니다. 분기에 한 번 정도:
**Table Editor → 각 표 → 우측 상단 ⋯ → Export to CSV** 로 내려받아 보관하세요.
이미지는 Storage → media 에서 다운로드할 수 있습니다.
