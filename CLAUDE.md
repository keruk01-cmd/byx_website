# CLAUDE.md

BYX 건축사사무소 포트폴리오 웹사이트. 작업 전 **`PROJECT.md` 를 먼저 읽으세요** —
전체 구조·디자인 시스템·의사결정 기록이 거기에 있습니다. 이 문서는 규칙 요약입니다.

## 프로젝트 성격

의존성 없는 **단일 HTML 파일** 사이트 + Supabase 백엔드. 빌드 단계가 없습니다.
`index.html` 을 브라우저로 열면 그게 곧 사이트입니다.

```
index.html    사이트 전체 (2,140줄, 폰트 base64 내장으로 528KB)
vercel.json   /admin 경로 rewrite
PROJECT.md    상세 인수인계 문서 ← 먼저 읽기
CHANGELOG.md  작업 로그 (`/wrap` 이 날짜별로 append)
배포-가이드.md  클라이언트용 운영 매뉴얼
```

## 가장 중요한 제약

클라이언트는 **건축가이며 개발 언어에 능숙하지 않습니다.**
GitHub 웹 편집기로 파일을 고치고 커밋하는 것이 유일한 배포 경로입니다.

따라서:

- **빌드 도구·프레임워크·npm 을 도입하지 마세요.** 제안도 하지 마세요
- **파일을 쪼개지 마세요.** `vercel.json` 이 유일한 예외
- **런타임 의존성을 늘리지 마세요.** 현재 외부 로드는 supabase-js 하나뿐
- 설정은 파일 맨 위 `window.BYX_CONFIG` (17~21줄) 한 곳에만

## 코드 규칙

- 뷰 함수는 템플릿 리터럴 **문자열을 반환**하는 순수 함수. 프레임워크 없음
- 사용자 입력은 반드시 `esc()` 로 이스케이프
- `render()` 가 `#app` 전체를 교체 → 이벤트는 `bindPage()` 에서 재바인딩
- ADMIN 입력 중에는 재렌더 금지 (포커스 유실). 구조 변경 시에만 `refreshAdmin()`
- 색은 `--bg`, `--fg`, `--mute`, `--faint` 넷뿐. **새 색을 추가하지 마세요**
- 좌우 여백은 `var(--pad)`, 경계선은 `var(--hair)` (1px)
- 폰트 크기는 `clamp()` 유동형. 미디어쿼리로 크기를 바꾸지 마세요
- 새 제목 스타일을 만들면 `font-variation-settings:'wght' 680` 셀렉터 목록에 추가
  (Anton에 한글이 없어 SUIT로 폴백될 때 굵기를 맞추는 장치)
- `svgArt()` 는 결정론적이어야 합니다. `Math.random()` 사용 금지
- 데모 모드 폴백(`sb === null`)을 제거하지 마세요. 클라이언트의 로컬 확인 수단입니다
- `settings` 표는 **없어도 되는 표**입니다. 조회 실패로 사이트를 데모 모드에 빠뜨리지 마세요
- 정렬값(`sort`)은 저장 시 날짜에서 계산합니다. 입력란을 되살리지 마세요
- 블로그 본문 저장 형태는 블록 배열 그대로. 편집 UI만 텍스트 한 칸입니다
- 업로드는 항상 `shrink()` 를 거칩니다 (2560px / WebP q0.82)
- `rich()` 는 반드시 `esc()` **뒤에** 돕니다. 순서를 뒤집으면 XSS 구멍입니다
- 캐러셀 이동에 `scrollTo({behavior:'smooth'})` 금지 — 애니메이션 불가 환경에서
  뒤이은 `scrollLeft` 대입까지 삼켜 슬라이드가 멈춥니다 (`PROJECT.md` §5-8)
- 본문 서식에 임의 색을 열지 마세요. `==강조==`(반전)와 `((흐리게))` 가 전부입니다

## 하지 말 것

- `service_role` / `secret` 키를 파일에 넣기
- 다크모드, 유형 필터, 문의 폼, 하단 CTA 재도입 (전부 의도적으로 삭제됨 — `PROJECT.md` §8)
- ADMIN SETUP 탭, 정렬값 입력란, 블로그 블록 추가·이동 UI 재도입 (§8)
- CONTACT의 대문짝 이메일·메일 3분할·소셜 버튼 재도입 (§8)
- 회색 배경 플레이스홀더 (`artOf()` 가 항상 그림을 반환합니다)

## 테스트

빌드가 없으므로 브라우저로 열어 확인합니다. 회귀 확인은 Playwright 스니펫이
`PROJECT.md` §10 에 있습니다. 최소 확인 항목:

- 전 라우트 렌더 + 콘솔 에러 0
- 데스크톱 1440 / 모바일 390 두 폭
- 슬롯머신 회전 → 정확히 정지, 밑줄 고정
- 그리드 무한 스크롤, INDEX 열 정렬 (Date 열은 연+월 기준)
- ADMIN 로그인 → 저장 → 공개 페이지 반영
- 로그인 시 각 페이지에 편집바 노출 / 로그아웃 시 미노출
- 블로그 본문 왕복 변환 무손실, 이미지 축소 동작

> 브라우저 확장으로 자동화할 때는 `file://` 이 막힙니다. 정적 서버로 띄워
> `http://localhost:...` 로 여세요. Supabase를 건드리지 않으려면 `BYX_CONFIG` 의
> url/key 를 비운 사본으로 시험하면 됩니다.

## 커뮤니케이션

- 사용자와의 대화는 **한국어**로
- 코드 주석은 영어 (기존 스타일 유지)
- 클라이언트용 문서(`배포-가이드.md`)는 비개발자 기준으로. 용어를 풀어서 쓰고
  버튼 이름을 그대로 적을 것
